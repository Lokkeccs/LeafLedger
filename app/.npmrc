# openapi-typescript (build-time codegen, P1-WP04) declares a peer of
# typescript@^5.x; this repo runs a newer typescript, which the tool uses fine
# via the backward-compatible compiler API. Accept the advisory peer mismatch
# consistently for both `npm install` and CI `npm ci`.
legacy-peer-deps=true
