# LeafLedger Rebuild — Status Tracker

> Master tracker. One row per work package. States: `planned | in-progress | verify | done | blocked`.
> Plan files: [plans/](./plans/). Specs: [docs/architecture/rebuild/](../architecture/rebuild/README.md). Playbook: [Part 7](../architecture/rebuild/07-vibe-coding-playbook.md).

## Phase 0 — Stabilize old system *(executed in the OLD repo `Lokkeccs/Accounting`)*

| WP | Title | State | Notes |
|---|---|---|---|
| P0-WP01 | Lock down /maintenance/* endpoints | done | Guard: Maintenance:Enabled flag (default OFF → 404) + AdminIdentities allowlist (403) + X-Maintenance-Confirm header (400). 8 unit tests. **Deployed to prod 2026-07-04** (health 200, endpoint 401/404). Old-repo commit pending. |
| P0-WP02 | Wire guard scripts into GitHub Actions (old repo) | planned | |
| P0-WP03 | Fix boundary violation (importUtils → data/fxConversion) | planned | |
| P0-WP04 | Verify/close export coverage (accounts, journal, master data) | planned | Self-migration path |
| P0-WP05 | Cosmos + blob backup; archive Accounting_docs | planned | |
| P0-WP06 | Usage probe: table rows per space (M1 vs M2 readiness) | planned | Sizes launch waves |

## Phase 1 — Repo + foundations

| WP | Title | State | Notes |
|---|---|---|---|
| P1-WP01 | Repo scaffold + CI skeleton (all gates blocking) | done | [plan](./plans/P1-WP01-repo-scaffold-ci.md) — QA PASS 2026-07-05; merged via PR #3 (all gates green), red-gate proven via PR #4 |
| P1-WP02 | docker-compose local dev (Postgres + API stub) | done | [plan](./plans/P1-WP02-docker-compose-local-dev.md) — QA PASS 2026-07-06; **merged to main via PR #5** (CI green). |
| P1-WP03 | SharedKernel: Money, Ids, Period, Result | done | [plan](./plans/P1-WP03-sharedkernel-value-types.md) — QA **PASS** 2026-07-06 (F1 fixed). Release build 0/0; 45/45 unit + 3/3 arch. [ADR-0002](./../architecture/adr/ADR-0002-id-storage.md). **Merged to main via PR #6.** |
| P1-WP04 | OpenAPI → generated TS client pipeline | done | [plan](./plans/P1-WP04-openapi-client-pipeline.md) — QA **PASS** 2026-07-06. Build-time OpenAPI doc → committed `backend/openapi/leafledger-v1.json`; `openapi-typescript`+`openapi-fetch` → `app/src/api/`; `getMeta()` + vitest; CI `contract` diff gate (red-gate independently reproduced). Release 0/0; arch 3/3; lint/typecheck/test/page-budget green; 0 vulns. **Merged to main via PR #8** (`24c21df`; rebased onto main after #6 squash-merge to clear stacked-branch conflicts). |
| P1-WP05 | ADR log + foundational ADRs (online-first + Postgres; contract pipeline) | done | [plan](./plans/P1-WP05-adr-log-foundational-adrs.md) — docs-only. [ADR log](../architecture/adr/README.md) created: **ADR-0001** (online-first + PostgreSQL + fresh-start/no-data-migration), **ADR-0003** (API contract pipeline), **ADR-0002** (ID storage, N1) registered. Rebuild README links the log. ADR-0002 byte-unchanged. **QA PASS 2026-07-06** (7/7 AC). Owner: LL Docs Editor. Ready for LL Git. |

## Phase 2 — Ledger core
*(Decomposed 2026-07-06 by LL Architect; **re-scoped 2026-07-11** — the original P2-WP05 was split into WP05 (posting/reversal endpoints), **WP09** (idempotency N2), and **WP10** (period lifecycle) to honor the ≤2-day rule. Fixtures-first — see playbook §5. Exit criterion (Part 4 §Phase 2): property-based financial-invariant suite green — any generated command sequence ⇒ trial balance ≡ 0; unbalanced / closed-period / invalid-ref posts rejected 422; retried posts exactly-once. Risk-review notes folded in at the named WP: N2 idempotency lifecycle → **P2-WP09**, N3 eager balance check → P2-WP05, N4 mat-view refresh → P2-WP07.)*

| WP | Title | State | Owner | Depends | Notes |
|---|---|---|---|---|---|
| P2-WP01 | Golden fixtures: posting-validity, currency-policy, period-state, FX-policy metadata | done | LL Fixture Smith | — | [plan](./plans/P2-WP01-golden-fixtures-posting-rules.md) — the porting oracle. **59 fixtures captured** from old-repo SHA `085bedb` (`postingValidity.ts`/`periodUtils.ts`/`fxPolicy.ts`) under `tests/fixtures/golden/ledger-core/` + committed harness `tools/fixtures/`. QA PASS 2026-07-06 (7/7 AC). **Merged to main via PR #11** (`.gitattributes` LF pin added for byte-identical re-capture across OS). |
| P2-WP02 | Postgres schema + EF migrations: spaces/accounts/groups/journal/lines/attributions/periods; RLS; deferred balance-constraint trigger; audit triggers | done | LL Backend Dev | P1-WP02, P1-WP03 | [plan](./plans/P2-WP02-postgres-schema-ef-migrations.md) — **schema/DDL only, no domain**. First backend module (`LeafLedger.Modules.Ledger`, single `LedgerDbContext` per **D1**, approved). `InitialLedgerSchema` migration: 9 tables + RLS (fail-closed, `app.current_space_id` GUC) + deferred balance trigger (N3 second wall) + append-only journal immutability grants + audit triggers (5 mutable tables, `app.current_actor`) + GiST code-range exclusion, all inline via `migrationBuilder.Sql`. **QA PASS re-verify 2026-07-10** (F1–F4 closed): all 9 ACs executed locally under Docker — **build 0/0; unit 45/45 + arch 3/3 + integration 37/37 green**. Carry-forward N-WP06-pool (per-request GUC binding). **Merged to main via PR #12** (`32fc5a7`, all CI green). |
| P2-WP03 | ChartOfAccounts domain: accounts, groups (int4range code ranges), currency policy, FX-policy resolution | done | LL Backend Dev | WP01, WP02 | [plan](./plans/P2-WP03-chart-of-accounts-domain.md) — pure `LeafLedger.Modules.ChartOfAccounts` domain: typed accounts/groups, inclusive code ranges, currency policy, FX defaults/overrides + metadata. **22/22 existing golden fixtures green** (11 currency + 11 FX) + 11 structural/loader tests. A1–A2 implemented: command-level currency guard remains WP05; investment historical is a default, not universal lock. No EF/context/schema/endpoints/frontend changes. **QA PASS 2026-07-11** — 10/10 ACs independently reproduced: Release build 0/0; full suite **118/118** (45 SharedKernel + 33 CoA + 3 arch + 37 integration, incl. no-model-drift). **Merged to main via PR #13.** |
| P2-WP04 | Ledger domain: JournalEntry aggregate (entry+lines+attributions), posting-validity rules, reversal linkage, immutability, exact-integer balance | done | LL Backend Dev | WP01, WP03 | [plan](./plans/P2-WP04-ledger-domain-journal-entry.md) — pure `LeafLedger.Modules.Ledger.Domain`: all **37** fixtures consumed (**36 exact + 1 ADR-0005 divergence**); immutable exact-integer aggregate; explicit-date reversal; optional/exact 1000‰ attribution (duplicate user rejected). **QA PASS 2026-07-11** — 12/12 ACs independently reproduced: Release **0/0**; full suite **176/176** (Ledger 58 + SharedKernel 45 + CoA 33 + arch 3 + PostgreSQL integration 37 incl. no-model-drift); golden fixtures byte-unchanged; no Infrastructure/schema/endpoint/frontend changes. **Merged to main via PR #14, all CI green.** |
| P2-WP05 | Posting & reversal application + endpoints; eager balance check (**N3** app half); currency/base composition; write-path tenancy binding | done | LL Backend Dev | WP02, WP03, WP04 | [plan](./plans/P2-WP05-posting-reversal-endpoints.md) — **QA PASS 2026-07-11** (12/12 ACs); **merged to `main` via PR #15, all CI green**. Re-scoped 2026-07-11 (idempotency→WP09, period lifecycle→WP10). `POST /journal-entries` (+`/reverse`); 422 structured ProblemDetails; both balance walls; txn-local RLS binding; monotonic, never-reused per-space `entry_no`. HTTP/TestServer coverage passes for 201/422/404/400 responses, stable ProblemDetails, reversal linkage, and route behavior. B1/B3 resolved; nine canonical B1 cases remain explicitly UNVERIFIED because the OLD repo has no executable base-amount validator. Production exposure remains gated by WP06 authorization and WP09 idempotency. |
| P2-WP06 | AuthZ pipeline: space role (typed) → module-permission endpoint filter; principal-bound RLS binding; license-entitlement seam | done | LL Backend Dev | WP02, WP05 | [plan](./plans/P2-WP06-authorization-pipeline.md) — **QA PASS 2026-07-11.** Typed `SpaceRole` Owner/Admin/Member/Viewer → `ledger.post`/`ledger.reverse` permission filter on the two WP05 endpoints; 401/403 ProblemDetails; principal-bound txn-local RLS binding replacing the trusted `X-Actor-Id` header. AuthN is configured JWT bearer + a test auth scheme (real Entra E2E/JWKS/identity-links → **P2-WP11**); license entitlement is a called seam (`ILicenseEntitlement`, allow-all default) — real enforcement owned by the **Licensing module**. Release backend `227/227`, focused authorization `41/41` and frontend gates green. No accounting decision / no golden fixtures (access control, not accounting). Per-endpoint filter tests; RLS as the second wall. |
| P2-WP07 | Reporting: trial-balance / balance-sheet / income-statement SQL views + `/integrity` hash | done | LL Backend Dev | WP02, WP05, WP06 | [plan](./plans/P2-WP07-reporting-views-integrity.md) — **implemented 2026-07-11**, **merged to main via PR #17 on 2026-07-12**. N4 mat-view refresh remains split out to **WP12**. Plain `security_invoker` views, database-defined C1 presentation amounts, transaction-local report service, deterministic hash endpoint, `ledger.read` for all roles, four read routes, regenerated OpenAPI/TS artifacts, and five report integration tests are complete. Final Release suite **236/236**; frontend gates green. **C1 resolved 2026-07-11**: raw debit-positive trial balance; presentation flips credit-normal liabilities/equity/income; expenses remain positive; explicit `net_result = income - expenses`; current result surfaced once in equity. `vat_report` is Phase 4. |
| P2-WP08 | Financial-invariant property suite (**Phase-2 exit gate**) | done | LL QA Reviewer | WP02–WP07, WP09, WP10 | [plan](./plans/P2-WP08-financial-invariant-property-suite.md) — **QA PASS 2026-07-12**. Focused property **15/15**; full Release backend **291/291** (integration **127/127**, architecture **3/3**); deterministic replay/shrinking, per-account model agreement, cross-space reverse isolation, DB trigger, and RLS properties verified. **Merged to `main` via PR #21 as `1aa35ae`; Phase 2 exit criterion met.** |
| P2-WP09 | Idempotency middleware (**N2**): `idempotency_keys` migration, client-ULID unique index, 24h TTL + cleanup, replay, collision metric | done | LL Backend Dev | WP05 | [plan](./plans/P2-WP09-idempotency-middleware.md) — QA findings addressed; PR #18 merged to `main` on 2026-07-12 as `fb1969c`. Cleanup uses a narrowly scoped security-definer purge function and is proven through `leafledger_app`; HTTP coverage includes reverse replay, expiry reuse, concurrent duplicates, failed-post retry, and cross-space isolation. Docker-backed full Release suite passed **247/247** (zero failures). **Sequence: WP09 → WP10 → WP08**. |
| P2-WP10 | Period-lifecycle commands: create / open / close / lock endpoints + onboarding open-period bootstrap (ADR-0005) | done | LL QA Reviewer | WP02, WP04, WP05, WP06, WP09 | [plan](./plans/P2-WP10-period-lifecycle.md) — QA PASS 2026-07-12; merged to `main` via PR #19. |
| P2-WP11 | AuthN token-validation hardening: Entra `common` JWKS signature + audience allowlist (fail-closed) + issuer-pattern allowlist bound to `tid` (§8.1) | done | LL Backend Dev | WP06 | [plan](./plans/P2-WP11-authn-token-validation.md) — **QA re-review PASS 2026-07-12**; implementation merged to `main` via **PR #22** as `20a8cc0`, documentation finalized via **PR #23** as `a660381`. Exact issuer parsing and real JwtBearer coverage for v1, malformed/missing issuers, and tenant allowlist behavior fixed. Focused authorization/JWT **41/41**; full Release backend **298/298** (integration **135/135**); architecture **3/3**. Identity-links → **P2-WP13**; frontend MSAL/`sessionStorage`/sign-out → Phase 3. |
| P2-WP12 | Materialized-view refresh strategy (**N4**) for the hot reporting views | done | LL QA Reviewer | WP07 | [plan](./plans/P2-WP12-materialized-view-refresh.md) — **QA PASS 2026-07-12**; merged to `main` via **PR #24** as `2b315fa`. Materialized/live equivalence, post-commit latency, coalescing, populated tenancy isolation, live-integrity/stale-dashboard behavior, concurrency, metrics, migration-down, and no-drift coverage all pass. Full Release backend **324/324** (`157` integration, `3` architecture); contract and diff hygiene checks clean. |
| P2-WP13 | Identity-links indirection: `identity_links` (Entra `sub`/`oid` + `tid` ↔ internal `user_id`) table + deterministic resolver replacing the WP06 `subject GUID == user_id` simplification | done | LL Backend Dev | WP06, WP11 | [plan](./plans/P2-WP13-identity-links.md) — **QA PASS and merged 2026-07-12 via PR #25 as `88a63a4`**. One raw-SQL migration (`identity_links` + `resolve_identity_link` **security-definer** function, table ungranted → app role EXECUTE-only; no EF entity/model drift); `IIdentityResolver` rewires membership and RLS actor binding to internal `user_id`; first contact provisions only the link, never membership. Consumers-tenant subject canonicalization prefers `sub`; missing `tid` returns `403 auth.identity_unresolved`. Validation: Release backend **332/332** (integration **165/165**, architecture **3/3**); focused identity/authorization coverage green; frontend lint/typecheck/tests/page-budget green; OpenAPI/TS regeneration no diff. No `Domain` or accounting changes. Frontend MSAL/session-state work remains Phase 3. |

## Phase 3 — Frontend re-platform
*(Decomposed 2026-07-12 by LL Architect into 8 WPs. Phase 2 is fully complete — P2-WP01–P2-WP13 all `done`; P2-WP11 (AuthN hardening), P2-WP12 (mat-view refresh), P2-WP13 (identity-links, PR #25) closed. Scope per Part 4 §Phase 3: new app shell — MSAL/`sessionStorage`, generated client, TanStack Query conventions, error boundaries, i18n corpus wholesale; port `shared/` UI primitives; accounts + journal-entry pages + trial-balance report against the new API, decomposed under page budgets, single posting flow. **Exit criterion (Part 4 §Phase 3): post a balanced entry → see it in the trial-balance report; two browsers in one space live-update via a SignalR invalidation ping.** Full M1 feature breadth (VAT, imports, master-data CRUD, admin, balance-sheet/P&L/dashboard, attachments) is **Phase 4**, not here. Risk-review folds: **N5** (SignalR coalescing) → P3-WP08, **N6** (invalidation map) → P3-WP08 (foundation laid in WP01 query-key convention). Frontend rules per [frontend.instructions.md](../../.github/instructions/frontend.instructions.md): desktop-first, page budgets, i18n-all-locales, minor-units-only, client validation is UX-only pinned by existing golden fixtures.)*

| WP | Title | State | Owner | Depends | Notes |
|---|---|---|---|---|---|
| P3-WP01 | App shell foundation: routing, TanStack Query provider + conventions, error boundaries, desktop-first layout frame | done | LL Frontend Dev | P1-WP04 | [plan](./plans/P3-WP01-app-shell-foundation.md) — approved 2026-07-13; accepted by LL QA Reviewer 2026-07-13; **merged to main via PR #26 on 2026-07-13**. Next: P3-WP02 (MSAL authentication).|
| P3-WP02 | MSAL authentication (`sessionStorage`) + bearer-token wiring + sign-out clears all app state | done | LL Frontend Dev | WP01, P2-WP11, P2-WP13 | [plan](./plans/P3-WP02-msal-authentication.md) — QA **PASS** on 2026-07-14; **merged to main via PR #27 as squash commit `75c0943`**. AC8 second-call throttle, AC1 all four Vite env overrides, and AC4 anonymous `getMeta()` through the shared generated client are directly proven. Fresh frontend Vitest **22/22**, lint/typecheck/build/page-budget green, production audit **0 vulnerabilities**, and no API artifact drift. Next: P3-WP03 (i18n corpus import).|
| P3-WP03 | i18n corpus import (EN + DE to start; FR/ES/IT deferred) + money/date/number formatting at the render edge | done | LL Frontend Dev | WP01, WP02 | [plan](./plans/P3-WP03-i18n-corpus-import.md) — QA PASS 2026-07-14; **merged to main via PR #28 on 2026-07-14**. Imported the local OLD EN/DE corpus, implemented i18next runtime/provider, launch keys, language persistence, render-edge formatters/hooks, duplicate-key build gate, shell translation migration, and focused coverage. Full frontend gates pass; backend Testcontainers regression was environment-blocked by unavailable Docker. Next: P3-WP04 (shared UI primitives and design tokens).|
| P3-WP04 | Shared UI primitives + design tokens (DataTable, modal, form primitives, generic pickers) | done | LL Frontend Dev | WP01, WP03 | [plan](./plans/P3-WP04-shared-ui-primitives.md) — QA PASS 2026-07-14; **merged to main via PR #29 as commit `38f7c88`**. `FormField` supports a generic control slot; token/source/non-port contract assertions are present. Full frontend Vitest **40/40**, lint/typecheck/page-budget/build/audit and scope checks green. Next: P3-WP05 planning/implementation. |
| P3-WP05 | Accounts read endpoint (backend) + dev seed + read-only accounts page | done | LL Backend Dev + LL Frontend Dev | WP01, WP04, P2-WP03, P2-WP06 | [plan](./plans/P3-WP05-accounts-read-endpoint-and-page.md) — WP05a backend Docker acceptance **6/6** and QA PASS. WP05b remediation closed both QA evidence findings: direct `useAccounts` query wiring plus router-level lazy `/accounts` and `RouteErrorBoundary` coverage. Full frontend Vitest `49/49`, lint/typecheck/page-budget/build/duplicate-key/audit/diff gates pass; generated contracts unchanged. **Merged to main via PR #30 on 2026-07-14.** Next: P3-WP06 planning/implementation. |
| P3-WP06 | Journal-entry page: single posting flow → `POST /journal-entries` + idempotency key + client-side balance pre-validation (UX-only, pinned by existing fixtures) | proposed | LL Frontend Dev | WP04, WP05, P2-WP05, P2-WP09 | The one posting flow (Part 3 §7). Client mirrors server balance/currency rules for instant feedback only — pinned by the P2-WP01/WP04 golden fixtures (thin TS mirror), server authoritative. Client-ULID idempotency key per submit. Page-budget decomposed (no `JournalEntryPage`-scale file). |
| P3-WP07 | Trial-balance report page (read path proof) | proposed | LL Frontend Dev | WP04, P2-WP07 | Minimal report page over `GET …/reports/trial-balance`; proves "post → see it". Balance-sheet / P&L / account-drill / dashboard are **Phase 4** (M1 Reports). |
| P3-WP08 | SignalR live invalidation: Azure SignalR ping + **N5** server coalescing + **N6** TanStack Query invalidation map/batching + **Phase-3 exit journey** | proposed | LL Backend Dev + LL Frontend Dev | WP06, WP07 | **Phase-3 exit gate.** N5: ≤1 ping/space/topic/~200 ms; N6: documented invalidation map + one batched refetch cycle per key + cache-correctness tests. Exit journey: post a balanced entry → trial balance updates → two browsers in one space live-update. Split seam WP08a (ping infra + server coalescing) / WP08b (client map/batching + exit journey) if it exceeds ≤2 days. |

## Phase 4 — Feature porting (M1 → M2)
## Phase 5 — Integrations
## Phase 6 — QA hardening
## Phase 7 — Launch & sunset

---

## Session log
| Date | WP | Agent | Result |
|---|---|---|---|
| 2026-07-14 | P3-WP05 | User / LL Architect | **Plan + all six decisions approved** on their recommended routes (D-P3-ACCOUNTS (a), D-P3-ACCT-MODULE, D-P3-ACCT-SEED, D-P3-ACCT-SPACE, D-P3-ACCT-PERM, D-P3-ACCT-DTO). No overrides. The user initially pasted approvals for the Phase-2 accounting decisions B1/B2/B3; LL Architect flagged that **B2 ("no gaps") and B3 ("gapless, per fiscal year") contradicted already-merged, QA-passed Phase-2 behavior** — P2-WP05 `entry_no` is monotonic/unique/never-reused per space with rollback gaps permitted and **no** fiscal-year partitioning, and P2-WP10 permits period gaps (undefined-date posts reject `posting_period.not_defined`); B1 (client-supplied base amount, server-validated) was already the merged behavior. The user withdrew B1/B2/B3 and confirmed the intent was to approve P3-WP05, so no merged Phase-2 accounting behavior was re-opened. Decisions are now implementation constraints; plan unblocked. State stays `planned`, ready for LL Backend Dev (WP05a). |
| 2026-07-14 | P3-WP05 | LL Backend Dev | Completed Docker-backed WP05a verification: focused account endpoint/seed suite **6/6** (ordered projection, authz, empty result, RLS isolation, seed idempotence); Host Release build and architecture `3/3` pass; frontend Vitest `40/40`, lint/typecheck/build, duplicate-key check, and diff hygiene pass. State → `verify`; next action is LL QA Reviewer acceptance review. |
| 2026-07-14 | P3-WP05 | LL QA Reviewer | **QA PASS for WP05a.** Independently reviewed scope, Ledger Infrastructure boundaries, RLS binding, authorization, Development seed gating/idempotence, contract artifacts, and patch layering. Reproduced focused Docker acceptance **6/6**, Ledger unit **86/86**, architecture **3/3**, Host Release build, frontend typecheck/build/lint gates, and diff hygiene. Parent WP05 remains `verify` pending WP05b frontend implementation. |
| 2026-07-14 | P3-WP05 | LL Frontend Dev | Implemented WP05b: application wrapper/query hook, shared `DataTable` accounts page, loading/empty/rows/error states, configured demo-space selection, lazy route and shell navigation, EN/DE copy, `.env.example` seam, and focused frontend coverage **6/6**. Full frontend Vitest **46/46**, lint, typecheck, page budget, duplicate-key check, and production build pass. State → `verify`; next action is LL QA Reviewer acceptance review. |
| 2026-07-14 | P3-WP05 | LL QA Reviewer | **QA FAIL for WP05b.** Frontend executable gates pass: Vitest `46/46`, lint/typecheck/page-budget/build/duplicate-key/audit/diff. Blocking evidence gaps: no direct `useAccounts` query-wiring test; no router-level `/accounts` resolution or route-error-boundary test. State → `in-progress`; next action is LL Frontend Dev remediation. |
| 2026-07-14 | P3-WP05 | LL QA Reviewer | **QA re-review PASS for WP05b.** Direct hook contract coverage and router/error-boundary coverage are present and pass. Focused remediation `3/3`; full frontend Vitest `49/49`, lint/typecheck/page-budget/build/duplicate-key/audit/diff gates pass; generated contracts unchanged. State → `verify`; next action is LL Git handling. |
| 2026-07-14 | P3-WP05 | LL Git | Rebased onto `origin/main`, committed as `14b545f`, pushed the WP05 branch, and opened PR #30. User confirmed PR #30 merged into `main`; WP05 → **done**. Next: P3-WP06 planning/implementation. |
| 2026-07-14 | P3-WP05 | LL Architect | Plan drafted → state `planned` (awaiting user approval). [P3-WP05 plan](./plans/P3-WP05-accounts-read-endpoint-and-page.md): the accounts read-path slice of Phase 3 — read-only `GET /api/v1/spaces/{spaceId}/accounts` (RLS-bound, reuses `ledger.read`) + a Development-only double-gated idempotent demo seed + a read-only `AccountsPage` through the WP04 `DataTable`. Researched Part 3 §Modules/§API (Chart of Accounts publishes catalog to Ledger; `CRUD …/accounts` — WP05 = read slice), Part 4 §Phase 3 + §5 (all data access = rewrite → TanStack Query + generated client), roadmap M1 (Accounts CRUD/import/built-in chart = **Phase 4**), playbook §107. Verified against real code: the `accounts` table + RLS policy + `leafledger_app` `SELECT` grant already exist (P2-WP02 `InitialLedgerSchema`) → **no migration**; the txn-local RLS-bound read pattern (`LedgerReportService.OpenBoundConnectionAsync`) + endpoint/authorization seam (`LedgerEndpoints`/`configureAuthorization`/`ledger.read`) are reusable; ChartOfAccounts is pure SharedKernel-only domain (no EF) → endpoint belongs in Ledger Infrastructure; `qk.accounts.list(spaceId)` already reserved; `getMeta`/`useMeta` set the application-wrapper+hook precedent; seed shape follows `LedgerDbFixture.SeedSpaceAsync`; `Program.cs` already has a Development-only `MigrateLedgerAsync()` gate. **Confirmed spec-derived rewrite of data access + a new server read capability + a dev-only seed; no golden fixtures, no accounting consult** (demo chart illustrative; normative built-in chart → Phase 4/expert). Six front-loaded non-accounting decisions for user sign-off: **D-P3-ACCOUNTS** (recommend (a) thin read endpoint + dev seed — the tracker decision), **D-P3-ACCT-MODULE** (endpoint in Ledger Infrastructure; ChartOfAccounts stays pure domain), **D-P3-ACCT-SEED** (Development-only double-gated idempotent demo seed; membership via configurable `Seed:DevUserId`), **D-P3-ACCT-SPACE** (`VITE_DEMO_SPACE_ID` demo space, defer real picker), **D-P3-ACCT-PERM** (reuse `ledger.read`), **D-P3-ACCT-DTO** (lean `AccountView`). Recommended WP05a (backend) → WP05b (frontend) split seam. 12 concrete ACs. |
| 2026-07-14 | P3-WP04 | User / LL Architect | **All six decisions + the plan approved** on their recommended routes — D-P3-UI-TOKENS (canonical `tokens.css`, migrate shell props, no visual change), D-P3-UI-RESPONSIVE (drop table breakpoint / `compactCard`), D-P3-UI-AMOUNT (rewrite money input integer-safe on WP03 `formatMoney`), D-P3-UI-THEME (defer theming → Phase 4), D-P3-UI-PICKERS (generic `DateField` only; domain pickers → WP06), D-P3-UI-BOUNDARY (add `app/src/shared/**` leaf ESLint rule). No overrides. Decisions are now implementation constraints; plan unblocked. State stays `planned`, ready for LL Frontend Dev. |
| 2026-07-14 | P3-WP04 | LL Frontend Dev | Implemented shared tokens/primitives and integer-safe money input; added 9 focused tests and the shared-leaf boundary. Full frontend Vitest **37/37**, lint/typecheck/page-budget/build/duplicate-key check green; `npm audit --omit=dev` **0 vulnerabilities**. No API/backend/OpenAPI/dependency changes. State → `verify`; next action is LL QA Reviewer acceptance review. |
| 2026-07-14 | P3-WP04 | LL QA Reviewer | **QA FAIL → in-progress.** Reproduced Vitest **37/37**, lint/typecheck/page-budget/build/audit and scope checks. Findings: `FormField` is hard-coded to `InputHTMLAttributes<HTMLInputElement>`/`<input>` instead of a generic control slot; token-layer smoke coverage is absent; required source/repository assertions for desktop-only/no-float/non-port constraints are absent. Required next action: LL Frontend Dev remediate and rerun the gates. |
| 2026-07-14 | P3-WP04 | LL Frontend Dev | Remediated all QA findings: generic `FormField` control slot plus token/source/non-port contract tests. Focused remediation **12/12**; full frontend Vitest **40/40**, lint/typecheck/page-budget/build/audit/scope checks green. State → `verify`; next action is LL QA Reviewer re-review. |
| 2026-07-14 | P3-WP04 | LL Architect | Plan drafted → state `planned` (awaiting user approval). [P3-WP04 plan](./plans/P3-WP04-shared-ui-primitives.md): the shared UI primitive + design-token slice of Phase 3. Researched §2 salvage verdict (React UI primitives = **Keep/salvage**), §7 (`shared/` = UI primitives + i18n + formatting; features→application→api), §5 (React UI primitives = salvage; **all data access = rewrite**), the desktop-first decision + frontend rules (**no responsive-table machinery**; reuse shared primitives before new UI; design tokens via CSS custom properties; **no float on money**), roadmap M1 (theming/format-prefs = Phase-4 admin surface), and playbook §107. Inspected the local OLD checkout `src/shared/` with exact refs: **salvage** (`DataTable.tsx` [strip `useIsTightTableScreen`/`compactCard`], `tableStyles.ts`, `listPrimitives.tsx`, `ModalShell.{tsx,module.css}`, `FormSection.tsx`, `ToggleSwitch.{tsx,module.css}`, optional `useToast.tsx`), **rewrite** (`AmountInput.tsx` uses `parseFloat`+`NumberFormatContext` → integer-safe `MoneyInput` on the WP03 `formatMoney`), **non-port** (`useTableBreakpoint.ts`; `NumberFormatContext`/`DateFormatContext`/`useNumberFormat`/`useDateFormat`/`spacePreferencesApi`; `ThemeContext`/`useTheme`), **defer** (`AccountPicker*`/`useRecentAccounts` → WP06; `DateFilterPicker`/`ReportingDateRangePickerControl`/`FilterBar`/`TopTabs`/`ViewSwitcher`/`CameraModal` → WP07/Phase 4). Cross-checked the real WP01 shell (`index.css` ad-hoc `--ink/--paper/…` tokens; `AppLayout.tsx`), the WP03 render-edge formatters, `eslint.config.js` (no `shared/**` rule yet — i18n leaf precedent), and the page-budget tool (targets only `App.tsx`/`*Page.tsx`). **Confirmed spec-derived salvage + token synthesis, no golden fixtures, no accounting consult.** Six front-loaded non-accounting decisions for user sign-off: **D-P3-UI-TOKENS** (canonical `tokens.css`, migrate shell props, no visual change), **D-P3-UI-RESPONSIVE** (drop table breakpoint / `compactCard`), **D-P3-UI-AMOUNT** (rewrite money input integer-safe), **D-P3-UI-THEME** (defer theming → Phase 4), **D-P3-UI-PICKERS** (generic `DateField` only; domain pickers → WP06), **D-P3-UI-BOUNDARY** (add `app/src/shared/**` leaf ESLint rule). 10 concrete ACs; WP04a/WP04b split seam documented. State `planned`; implementation blocked on decision sign-off. | (FR/ES/IT deferred to a content-only follow-up), a deliberate recorded divergence from the roadmap "all 5 locales from day one" line. D-P3-I18N-LIB (i18next + react-i18next), -PERSIST (`localStorage` `ll.language`, exempt from sign-out teardown), -FORMAT (render-edge `Intl` keyed to active locale, integer-safe money; space-preference overrides → Phase 4), -CORPUS (verbatim import) accepted on recommended routes. Plan updated throughout: scope/goal/context/file-list/tests/ACs now reference the two launch locales; i18next runtime built locale-count-agnostic so adding FR/ES/IT later is a pure content import; new carry-forward for the deferred FR/ES/IT follow-up (propose as P3-WP03b or a Phase-4 content item). State stays `planned`, ready for LL Frontend Dev. |
| 2026-07-14 | P3-WP03 | LL Frontend Dev | Implemented the i18n provider/runtime, imported the local OLD EN/DE corpus, added launch keys, language persistence, render-edge formatters/hooks, duplicate-key build gate, translated shell, and focused tests. QA remediation added scoped duplicate-key regression coverage, persistence-after-cache-clear coverage, and active-locale key coverage. Full Vitest **28/28**, lint, typecheck, strict page budget, production build, duplicate-key check, and npm audit (**0 vulnerabilities**) pass; no API/backend scope drift. State → **verify**; next action is LL QA Reviewer acceptance review. |
| 2026-07-14 | P3-WP03 | LL QA Reviewer | **QA PASS → done.** Independently verified corpus fidelity against the local OLD checkout, scoped duplicate-key behavior, shell/provider wiring, integer-safe formatting, persistence teardown exemption, and no generated/backend scope drift. Full frontend Vitest **28/28**, lint, typecheck, strict page budget, production build, duplicate-key check, and npm audit (**0 vulnerabilities**) pass. Repository-wide backend Testcontainers regression was attempted but Docker was unavailable; 129 integration setup failures are recorded in the plan as an environment limitation. Next: LL Git handling. |
| 2026-07-14 | P3-WP03 | LL Git | PR #28 merged into `main`. WP03 complete; next action is P3-WP04 (shared UI primitives and design tokens). |
| 2026-07-14 | P3-WP03 | LL Architect | Plan drafted → state `planned` (awaiting user approval). [P3-WP03 plan](./plans/P3-WP03-i18n-corpus-import.md): the i18n + render-edge-formatting slice of Phase 3. Researched §2 salvage line 45 (**i18next + 5-locale corpus = Keep**; duplicate-key check stays in build), §7 (`shared/` = i18n ×5 + formatting; features→application→api), §5 (salvage the corpus; **rewrite** all data access → the OLD space-preference number/date machinery is a non-port), roadmap M1 Shell (all 5 locales day one, corpus wholesale; *format prefs* are the Phase-4 admin surface), §5-quality (duplicate-key check + all-5-locales PR rule), playbook §107, and the OLD code with exact refs (`src/i18n/index.ts` init shape — `initReactI18next`/`resources`/`fallbackLng:'en'`/`escapeValue:false`/`setLanguage` + `localStorage 'app-language'`; `src/i18n/locales/{en,de,fr,es,it}.json`; `scripts/check-i18n-duplicate-keys.cjs`; OLD `App.tsx` `I18nextProvider` composition; the `useNumberFormat`/`useDateFormat`/`spacePreferencesApi` space-preference machinery flagged **non-port**; noted OLD `it.json` carries some ES strings) — confirmed **spec-derived salvage of the corpus + greenfield formatters, no golden fixtures, no accounting consult**. Cross-checked the real WP01/WP02 shell (`providers.tsx` provider tree + i18n slot; `AppLayout` hard-coded chrome; generated schema money as `number` minor units) and `eslint.config.js` (a shared `src/i18n` leaf needs no boundary change). Scope: verbatim 5-locale import, i18next runtime + outermost `I18nextProvider`, ported duplicate-key `build` guard (red-gate), shell-string→key replacement in all 5 locales, integer-safe `formatMoney` + locale date/number formatters + hooks, `localStorage` language pref decoupled from the WP02 sign-out teardown. Four front-loaded non-accounting decisions for user sign-off: **D-P3-I18N-LIB** (keep i18next + react-i18next), **D-P3-I18N-PERSIST** (language in `localStorage`, exempt from sign-out teardown), **D-P3-I18N-FORMAT** (render-edge `Intl` keyed to active locale, integer-safe money; space-preference overrides → Phase 4), **D-P3-I18N-CORPUS** (verbatim import incl. defects). New prod deps recorded: `i18next`, `react-i18next`. 10 concrete ACs; no backend/OpenAPI/`app/src/api/**` change. Implementation blocked until the four decisions are approved. |
| 2026-07-14 | P3-WP02 | LL Git | PR #27 squash-merged into `main` as `75c0943`. WP02 complete; next action is P3-WP03 (i18n corpus import). |
| 2026-07-14 | P3-WP02 | LL QA Reviewer | **QA PASS → done.** Closed the three follow-up evidence gaps: AC8 second-call popup throttle, AC1 Vite environment overrides, and AC4 anonymous generated-client metadata integration. Fresh frontend Vitest **22/22**, lint/typecheck/build/page-budget green, production audit **0 vulnerabilities**, and OpenAPI artifact stability pass. Next: LL Git commit/PR handling.|
| 2026-07-14 | P3-WP02 | LL Frontend Dev | Fixed the three QA follow-up coverage gaps: second-call popup throttle assertion, all four MSAL/API Vite env override assertions, and anonymous `getMeta()` through the shared generated client with no bearer header. Full frontend Vitest **22/22**, lint/typecheck/build/page-budget green, production audit **0 vulnerabilities**, and OpenAPI artifact stability pass. State → **verify**; next action is LL QA Reviewer re-review. |
| 2026-07-13 | P3-WP02 | LL QA Reviewer | **QA follow-up FAIL → in-progress.** Original sign-out/cache and missing-core-coverage findings are closed. Remaining blocking evidence gaps: no second-call throttle assertion for AC8, no `VITE_` environment override assertions for AC1, and no actual generated-client `getMeta()` anonymous integration assertion for AC4. Full frontend Vitest **21/21**, lint/typecheck/build/page-budget green, production audit **0 vulnerabilities**, and OpenAPI artifact stability pass. Required next action: add the three focused tests, then re-review. |
| 2026-07-13 | P3-WP02 | LL QA Reviewer | **QA re-review PASS → done.** Fixed F1: `signOut()` clears MSAL cache after `logoutPopup()` even when the popup rejects, and an explicit signed-out latch prevents cached accounts from restoring the shell. Fixed F2: added provider/init-gate, sign-in, failed-popup sign-out teardown, configured-state, and no-auto-join acceptance coverage. Full frontend Vitest **21/21**, lint/typecheck/build/page-budget green, production audit **0 vulnerabilities**, and OpenAPI artifact stability pass. Next: LL Git commit/PR handling. |
| 2026-07-13 | P3-WP02 | LL QA Reviewer | **QA FAIL → in-progress.** Full frontend Vitest **16/16**, lint, typecheck, strict page budget, production build, production audit (**0 vulnerabilities**), and OpenAPI artifact stability pass. Blocking F1: `useAuth.signOut()` leaves MSAL cached accounts intact when `logoutPopup` rejects, allowing `accounts[0]` to restore signed-in state. Blocking F2: missing provider/init-gate, sign-in, sign-out teardown, configured-state, and no-auto-join acceptance tests. Required next action: fix F1 and add F2 coverage, then re-review. |
| 2026-07-13 | P3-WP02 | LL Frontend Dev | Implemented MSAL popup authentication with `sessionStorage`, plain `initialize()` gate, `@azure/msal-react` provider, silent-token plus throttled popup fallback, generated-client bearer middleware, sign-in/out shell chrome, full query cache teardown, configured-vs-not guard, and mock-focused auth tests. No backend/OpenAPI/generated schema/client auto-join changes. Validation: focused auth **4/4**, full frontend Vitest **16/16**, lint, typecheck, production build, strict page budget, and production audit **0 vulnerabilities**. State → **verify**; next action is LL QA Reviewer acceptance review. |
| 2026-07-13 | P3-WP02 | User / LL Architect | **Plan approved with a decision override: D-P3-MSAL-FLOW = popup** (recommended was redirect). D-P3-MSAL-LIB (`@azure/msal-react`+`@azure/msal-browser`), D-P3-MSAL-BEARER (`openapi-fetch` `onRequest` middleware), D-P3-MSAL-SIGNOUT (full clear) accepted on recommended routes; D-P3-MSAL-REG accepted mock-verified (no Entra SPA client id supplied → live E2E sign-in deferred to the WP08 exit gate). Plan updated to popup semantics: pre-mount redirect gate collapses to a plain `initialize()` gate (no `?code=` main-window round-trip); `signIn=loginPopup`, `signOut=logoutPopup`, silent-then-`acquireTokenPopup` fallback; interactive calls require a user gesture and the gesture-less bearer middleware surfaces a single throttled "interaction required" error instead of per-request popups. ACs 2/5/6/8, Goal, Scope §2/§4/§5, source-material, file list, and tests revised accordingly. State `planned`, ready for LL Frontend Dev. |
| 2026-07-13 | P3-WP02 | LL Architect | Plan drafted → state `planned`. [P3-WP02 plan](./plans/P3-WP02-msal-authentication.md): the authentication slice of Phase 3. Researched §8.1 (Entra `common`; **MSAL cache `sessionStorage`**; **sign-out clears all app state / kills the stale-space auto-join class**), §2 salvage line 51 (MSAL=Keep, cache→`sessionStorage`, identity-links replaces candidate guessing), §5 (salvage MSAL *flow shape*; rewrite all data access), §7 (app-shell owns providers; `shared/` = auth glue; **features→application→api**), and the OLD code with exact refs (`src/auth/msalConfig.ts` `MSAL_CONFIG` `cacheLocation:'localStorage'`+`LOGIN_SCOPES`/`API_SYNC_SCOPE`; `src/auth/AuthContext.tsx` `primeAuthentication`/`handleRedirectPromise` redirect-before-mount, `signIn=loginRedirect`, `signOut=logoutRedirect`, `getAccessToken=acquireTokenSilent`+`InteractionRequiredAuthError` throttled fallback, and the `upsertMicrosoftUser` auto-join weakness; `tests/authRedirectFlow.test.ts` mock pattern) — confirmed **spec-derived rewrite of a salvaged flow shape, no golden fixtures, no accounting consult**. Cross-checked the real backend contract (`appsettings.json`: audience `api://leafledger`, `RequiredScope=ledger.write`, authority `common/v2.0`) → SPA requests `api://leafledger/ledger.write`; and the WP01 `AppRoot` MSAL provider slot + hand-authored `app/src/api/client.ts` (bearer middleware seam; `schema.d.ts` is the generated file). Scope: env-driven MSAL config (`sessionStorage`), provider mount + init/redirect gate above the router, `openapi-fetch` `onRequest` bearer middleware (anonymous `getMeta()` preserved), `signIn`/`signOut` with full teardown (`queryClient.clear()`), throttled interactive fallback, explicit **non-port** of the client auto-join. Five front-loaded non-accounting decisions for user sign-off: **D-P3-MSAL-REG** (Entra SPA app registration — public client id/redirect URIs/API-scope grant as `VITE_` env, no secrets; provide now for live smoke **or** defer live E2E to WP08 — implementation is fully MSAL-mock-verified either way), **D-P3-MSAL-LIB** (`@azure/msal-react`+`@azure/msal-browser`), **D-P3-MSAL-FLOW** (redirect), **D-P3-MSAL-BEARER** (middleware), **D-P3-MSAL-SIGNOUT** (full clear). New prod deps + four `VITE_MSAL_*`/`VITE_API_SCOPE` env vars recorded before use; no backend/OpenAPI-TS change. 12 concrete ACs. Awaiting user approval. |
| 2026-07-12 | Phase 3 | LL Architect | **Decomposed Phase 3 (Frontend re-platform) into 8 WPs** (P3-WP01…08). Researched Part 3 §7 (frontend structure: features→application→api; TanStack Query replaces Dexie/DataApi; app layer owns shell/routing/providers/error boundaries; desktop-first, no `useTableBreakpoint` port) + §8.1 (MSAL/`sessionStorage`/sign-out) + §9 (error boundary per route + top level), Part 4 §Phase 3 (app shell + accounts/journal pages + trial-balance + two-browser SignalR exit) + §5 (salvage: UI primitives/i18n/MSAL-shape; rewrite: all data access → TanStack Query + generated client), Part 6 M1 (**Phase 3 = shell + first vertical slice only; VAT/imports/master-data-CRUD/admin/BS-P&L-dashboard are Phase 4**), the risk-review N5/N6, and the frontend instructions (page budgets, layering, i18n-all-locales, minor-units-only). WP breakdown: **WP01** app-shell foundation (this draft), **WP02** MSAL auth, **WP03** i18n corpus, **WP04** shared primitives + tokens, **WP05** accounts read endpoint + dev seed + accounts page, **WP06** journal-entry single posting flow, **WP07** trial-balance report page, **WP08** SignalR live invalidation (**N5** coalescing + **N6** map) + **Phase-3 exit journey**. Folded **N5**→WP08, **N6**→WP08 (foundation in WP01 query-key factory). Surfaced dependency **D-P3-ACCOUNTS** (no `GET accounts` endpoint/seed exists yet → WP05 adds the first ChartOfAccounts read endpoint). Drafted [P3-WP01 plan](./plans/P3-WP01-app-shell-foundation.md) in full: providers + router + TanStack Query conventions (client defaults + query-key factory) + two error boundaries + desktop-first layout frame + `getMeta()` wiring proof; 10 concrete ACs; **no backend / no accounting consult / no golden fixtures**. Three front-loaded non-accounting decisions need user sign-off before implementation: **D-P3-STRUCT** (keep flat `app/src` for P3 — recommend defer the `app/web`+`app/shared` split to M3/companion), **D-P3-ROUTER** (recommend React Router data router), **D-P3-QUERYKEYS** (typed query-key factory — the N6 foundation). New frontend prod deps recorded: `@tanstack/react-query` + the chosen router. Awaiting approval of P3-WP01. |
| 2026-07-13 | P3-WP01 | User / LL Docs Editor | User approved the recommended decisions: **D-P3-STRUCT** keep flat `app/src` during Phase 3, **D-P3-ROUTER** use React Router data router, and **D-P3-QUERYKEYS** use the module-namespaced typed query-key factory. P3-WP01 plan updated; no implementation changes. WP01 remains `planned`, ready for LL Frontend Dev. |
| 2026-07-13 | P3-WP01 | LL Frontend Dev | Extended implementation with DOM-level coverage for home/404 routing, `getMeta()` query rendering, layout focusability, generic/ProblemDetails route fallbacks, and top-level error logging. Independent acceptance pass complete: boundary negative probe rejected as expected, no responsive-table/direct-fetch shell machinery, OpenAPI regeneration byte-stable, frontend lint, Vitest **12/12**, page budget, typecheck/build, and production audit (0 vulnerabilities) pass. WP01 → **verify**; next action is LL QA Reviewer acceptance review. |
| 2026-07-03 | — | (seeding) | Repo cloned; specs, agents, instructions, tracker, P1-WP01 plan seeded |
| 2026-07-04 | P1-WP01 | LL Backend Dev | Backend scaffold: sln, Directory.Build.props, SharedKernel (empty), Host `/health` (200 verified), arch tests 3/3 green |
| 2026-07-04 | P1-WP01 | LL Frontend Dev | Frontend scaffold, boundary ESLint, vitest, page-budget tool, workflows, root hygiene; all local gates green; boundary rules verified to fail |
| 2026-07-05 | P1-WP01 | LL QA Reviewer | PASS → done: verified on merged main (backend 3/3, frontend lint/typecheck/test/budget green); PR #3 CI all-green (criterion 3); PR #4 red-gate proven (criterion 4) |
| 2026-07-05 | P1-WP02 | LL Architect | Plan drafted: docker-compose (Postgres+API stub), DB readiness health check, Testcontainers integration pattern (main-branch gate); awaiting approval |
| 2026-07-06 | — | LL Architect | External architecture risk review triaged with user: 6 items adopted (N1–N6), 3 deferred, rest already covered/rejected → [plans/NOTES-risk-review-2026-07-06.md](./plans/NOTES-risk-review-2026-07-06.md); WP rows annotated |
| 2026-07-06 | P1-WP02 | LL Backend Dev | Implemented compose/Dockerfile/health/integration/CI/README; Release build clean, arch 3/3; integration fails locally (no Docker). Moved to verify. |
| 2026-07-06 | P1-WP02 | LL QA Reviewer | FAIL → in-progress: F1 solution-wide `dotnet test` runs Testcontainers on PRs (violates non-goal); F2 Dockerfile `dotnet restore sln` fails (MSB3202, missing test csproj → image won't build). Both reproduced locally. Build Release clean; arch 3/3. |
| 2026-07-06 | P1-WP02 | LL Backend Dev | Fixed F1 (`[Trait("Category","Integration")]` + `--filter "Category!=Integration"` on backend job in pr.yml/main.yml) and F2 (Dockerfile restores Host csproj only). Verified locally: filtered backend test 3/3 exit 0; Host-only restore exit 0. → verify. |
| 2026-07-06 | P1-WP02 | LL QA Reviewer | PASS → done (conditional on CI): F1/F2 re-verified — filtered backend test 3/3 exit 0 (integration excluded), Host-only restore exit 0, Release 0/0. AC1/2/3/6 (Docker runtime) CI-proven; no local Docker engine. Merge only on green CI. |
| 2026-07-06 | P1-WP02 | LL Git | Branch + commit `7cc83ac` + draft PR #5. User confirmed CI green and **merged to main**. |
| 2026-07-06 | P1-WP03 | LL Architect | Plan drafted: SharedKernel value types (Money/minor-units, ULID`Id<T>`↔uuid per N1, Period, Result). Greenfield rewrite (no golden fixtures; behavioral porting is Phase 2). ADR for ID storage. 2 open questions routed to LL Accounting Expert. Awaiting approval. |
| 2026-07-06 | P1-WP03 | LL Accounting Expert | Resolved open questions: (A) decimal→minor rounding = `AwayFromZero` (Swiss commercial rounding, per OR/ESTV practice; explicit API, no hidden default; legal VAT/FX rounding stays Phase 2); (B) `Period` = plain half-open `[start, endExclusive)` range + status (OR art. 958 flexibility met by the range primitive; fiscal-calendar derivation is Phase 2). Answers recorded in plan; ACs #3/#10 tightened. |
| 2026-07-06 | P1-WP03 | LL Backend Dev | Implemented Money/CurrencyCode/Id<T>/Period/Result + ADR-0002. Release build 0/0; 44/44 unit tests; 3/3 arch tests. In-house big-endian ULID↔uuid conversion (order-preserving, property-tested); `Ulid` 1.3.4 dep added to SharedKernel. Deviations: generative xUnit instead of CsCheck; `Error`→`DomainError` (CA1716); CA1000 suppressed on generic value types; CA1707 NoWarn in test project. → verify. |
| 2026-07-06 | P1-WP03 | LL QA Reviewer | FAIL → in-progress. Independently reproduced: Release 0/0, 44/44 unit + 3/3 arch, integration excluded, no scope creep. F1 (AC-4): no-float reflection test covers only `Money`, not `CurrencyCode` as the AC mandates (invariant holds; test-coverage gap). All other ACs + financial-integrity/security/hallucination/patch-layering checks PASS. |
| 2026-07-06 | P1-WP03 | LL Backend Dev | Fixed F1: `No_public_member_exposes_float_or_double` converted from `[Fact]` to `[Theory]` over `Money` and `CurrencyCode`. Release build 0/0; 45/45 unit + 3/3 arch pass; integration excluded. → verify. |
| 2026-07-06 | P1-WP03 | LL QA Reviewer | **PASS** (re-verify). F1 closed — no-float theory now covers `Money` + `CurrencyCode`. Independently reproduced: Release 0/0, 45/45 unit + 3/3 arch, integration excluded, no scope creep. All ACs met. → done. Ready for LL Git (exclude 3 unrelated agent-file deletions). |
| 2026-07-11 | P2-WP04 | LL Architect | Plan drafted → state `planned`. Pure Ledger.Domain: JournalEntry aggregate + posting-validity port + period-state + reversal + exact-integer balance + attribution 1000‰. Salvage (posting-validity accounts/partners/users/projects/period) pinned by the 37 remaining P2-WP01 fixtures (10+7+7+5+8) — no new fixtures/Fixture Smith. OLD-repo search confirmed **no reverse-entry function** (corrections were hard deletes) → reversal is spec-derived rewrite, pinned by unit + WP08 property. Boundary preserved (Domain takes plain reference snapshots; local `PostingPurpose`; no ChartOfAccounts/EF dep). Routed 3 accounting questions to LL Accounting Expert: **A1** period-required?, **A2** reversal date/period, **A3** attribution obligation. Implementation blocked until A1–A3 + approval. |
| 2026-07-11 | P2-WP04 | LL Accounting Expert | **A1–A3 resolved. A1:** require a defined period whose effective state is `open` for every posting; no-period rejects (deliberate divergence from one OLD fixture — preserve golden, add ADR + target-decision test; onboarding must create a period; overlap policy not decided here). **A2:** reversal takes an explicit effective date and must post in an open period; never silently backdate/reopen; preserve reversal link/audit trail. **A3:** attribution is optional; when present each share is 1..1000 and per-line sum exactly 1000‰; no line kind mandatory in WP04. Basis: OR 957a(2), OR 958f, GeBüV Art. 3; period master/attribution are internal-control product policies, not statutory data-model mandates. Accounting blocker cleared → user approval + A1 ADR/target artifact next. |
| 2026-07-11 | P2-WP04 | LL Docs Editor | Recorded the A1 divergence as **[ADR-0005](../architecture/adr/ADR-0005-posting-requires-open-period.md)** (accepted): posting requires a defined `open` period; `no-period-defined`/`closed`/`locked` reject; distinct code `posting_period.not_defined`; OLD golden fixture preserved and pinned as the single documented OLD→target divergence; onboarding/period-lifecycle wiring → WP05; overlap policy out of scope. Registered in the [ADR log](../architecture/adr/README.md) (ADR-0004 kept reserved for the P2-WP02 N-D1 single-context decision, not reused). Linked from the P2-WP04 plan A1 + DoD. Remaining blocker: user approval. |
| 2026-07-11 | P2-WP04 | LL Backend Dev | User approved the plan after A1–A3 sign-off and ADR-0005. Created branch `P2-WP04-ledger-domain` from merged `origin/main` (`3f7fd34`). State → in-progress; fixture-first TDD started. |
| 2026-07-11 | P2-WP04 | LL Backend Dev | **Implemented → verify.** TDD red baseline (planned missing Domain types), then pure Ledger.Domain: posting-validity + period port consumes **37/37** fixtures (36 exact + ADR-0005 no-period divergence); immutable JournalEntry aggregate; overflow-safe exact-integer base balance; explicit-date linked reversal preserving line metadata/attributions; optional attribution summing 1000‰ with duplicate user rejected. Architecture now explicitly isolates Ledger.Domain from CoA/EF/ASP.NET/Host. **Results:** clean Release build **0/0**; full suite **176/176** (Ledger 58 + SharedKernel 45 + CoA 33 + arch 3 + PostgreSQL integration 37 incl. no-model-drift). Scope clean: no Infrastructure/migration/schema/Host/API/frontend/workflow edits. → LL QA Reviewer. |
| 2026-07-11 | P2-WP04 | LL QA Reviewer | **PASS → done.** Independently reproduced from a clean `--no-incremental` rebuild: Release **0/0**; full suite **176/176** (SharedKernel 45 + CoA 33 + **Ledger 58** + arch 3 + PostgreSQL integration 37 incl. `HasPendingModelChanges()` no-drift). Re-ran by name: 37-fixture selection, 8 period-state (incl. `ps-no-period-allowed` ADR-0005 divergence preserving OLD `ok` + proving `posting_period.not_defined`), no-float theory, explicit-reversal-date, long-boundary balance = 14/14. All 12 ACs met: posting-validity asymmetries match the OLD oracle (business `isActive:false` passes; inactive personal-only; window business-only; inclusive boundaries; multi-issue order); exact-integer balance via `BigInteger` (overflow-safe); immutable aggregate; `Reverse` negates both amounts, preserves metadata/attributions/reference, links `ReversesEntryId`, requires explicit date, original unchanged; attribution optional/1..1000/sum 1000‰/duplicate rejected. **Golden fixtures byte-unchanged** vs `origin/main`; diff limited to sln + Ledger.csproj + `ModuleBoundaryTests.cs` + adr README + status (+ new Domain/tests/ADR-0005/plan). Financial: minor-units only, no float/decimal, immutability + reversal-only corrections, idempotency correctly deferred to WP05. Security: no endpoints (authz→WP05/06), no secrets. Patch-layering: named guards only (no generic catch). Non-blocking carry-forwards: (1) WP05 standardize ProblemDetails codes (`posting_period.not_open` internal-only); (2) WP08 pin exact period boundary inclusivity; (3) description validation is a WP05 request-boundary concern. Cleared for LL Git. |
| 2026-07-06 | P1-WP04 | LL Architect | Plan drafted: single-contract pipeline. Backend build-time OpenAPI doc (`Microsoft.AspNetCore.OpenApi` + `Microsoft.Extensions.ApiDescription.Server`) → committed `backend/openapi/leafledger-v1.json`; `/api/v1/meta` anchor endpoint; frontend `openapi-typescript`+`openapi-fetch` → `app/src/api/` (types + stable `client.ts`); application-layer `getMeta()` wrapper + vitest; CI `contract` diff gate (drift-killer) with red-gate proof. No golden fixtures / no accounting decision (pure tooling). Tool choice flagged as candidate ADR-0003 for P1-WP05. Awaiting approval. |
| 2026-07-06 | P1-WP04 | LL Backend Dev | Backend half implemented: Host OpenAPI packages + build-time doc emission → committed deterministic `backend/openapi/leafledger-v1.json` (byte-identical across `--no-incremental` regens); `/api/v1/meta` → `MetaResponse{name,version}` (both required strings); doc transformer sets `info.title/version=v1`; filename normalized via MSBuild `Move` target (tool has no filename knob). Release build 0/0; arch 3/3. → frontend gen + CI gate next. |
| 2026-07-06 | P1-WP04 | LL Frontend Dev | Frontend half implemented: `openapi-typescript`+`openapi-fetch` → `app/src/api/{schema.d.ts,client.ts}`; `gen:api` script; application-layer `getMeta()` + vitest (map + throw); CI `contract` diff gate in pr.yml/main.yml (build → gen → `git add -N` + `git diff --exit-code`), wired into main deploy `needs`. **Red-gate proven**: drift exit 1 (both artifacts) / restored exit 0 (deterministic). Fixes: `.npmrc legacy-peer-deps` (tool peer TS^5 vs repo TS6), `.gitattributes` LF pin. Gates green: lint/typecheck/test(3)/page-budget; npm audit 0 vulns; dotnet vulnerable none. → verify. |
| 2026-07-06 | P1-WP04 | LL QA Reviewer | **PASS → done.** Independently reproduced all 10 ACs: Release 0/0, arch 3/3, contract `GET /api/v1/meta`+`MetaResponse{name,version}`/`info.version=v1`, `schema_fresh=True`, vitest 3/3, lint/typecheck/page-budget green, npm audit 0 / dotnet vulnerable none. Red-gate independently reproduced (source mutation changes both artifacts; restore byte-identical). Security: OpenAPI doc Dev-only, no secrets, no-input anchor. Non-blocking notes: N1 `.gitattributes`/`.npmrc` outside file-list (documented deviations), N2 branch hygiene for LL Git, N3 meta endpoint unauth (acceptable, auth is Non-goal). Cleared for LL Git (branch P1-WP04; 16-path set). |
| 2026-07-06 | P1-WP03 | LL Git | Branch `P1-WP03-sharedkernel-value-types` + PR #6 **squash-merged to main** (`f5e9fa5`). |
| 2026-07-06 | P1-WP04 | LL Git | Committed 16-path set to `P1-WP04-openapi-client-pipeline` (`dcaa253`), pushed, opened PR #7 (stacked on P1-WP03). After #6 squash-merged, stacked PR #8 (base main) showed conflicts (`Period.cs`, `status.md`); rebased branch `--onto origin/main` (dropped redundant P1-WP03 commits → single commit `8123de3`), force-pushed with lease → PR #8 CLEAN/MERGEABLE. #7 had already merged into the orphaned P1-WP03 branch (harmless). **PR #8 squash-merged to main** (`24c21df`). |
| 2026-07-06 | — | LL Git | Docs sync for P1-WP04 committed via docs branch `docs-P1-WP04-status-sync` → **PR #9 merged to main** (`64564a2`). |
| 2026-07-06 | P1-WP05 | LL Architect | Plan drafted: ADR log/index + **ADR-0001** (online-first + PostgreSQL; no client-side schema/migrations) + **ADR-0003** (API contract pipeline, retroactive from P1-WP04); register already-accepted **ADR-0002** (ID storage, N1). Docs-only, owner LL Docs Editor, ≤ 1 day. No accounting consult / no golden fixtures (pure architecture docs). Resolved stale tracker note (ID-storage ADR already delivered). 7 verifiable acceptance criteria (index 1:1 with files, template compliance, spec-faithful decisions, no broken links, docs-only). Awaiting approval. |
| 2026-07-06 | P1-WP05 | LL Docs Editor | Implemented: created [adr/README.md](../architecture/adr/README.md) (index: convention, lifecycle, 3-row log), [ADR-0001](../architecture/adr/ADR-0001-online-first-postgres.md) (accepted; online-first + PostgreSQL + fresh-start/no-bulk-migration — all three co-decided 2026-07-03 per rebuild README headline #5), [ADR-0003](../architecture/adr/ADR-0003-api-contract-pipeline.md) (accepted; build-time OpenAPI + openapi-typescript/openapi-fetch, no hook codegen). Registered unchanged ADR-0002; linked log from rebuild README. Refinement vs plan: recorded the no-**data**-migration launch decision inside ADR-0001 (spec treats it as a co-decided foundational decision, not a Phase-7-only detail) while still not creating a separate cutover ADR. Docs-only diff. → verify. |
| 2026-07-06 | P1-WP05 | LL QA Reviewer | **PASS** — 7/7 acceptance criteria met, independently reproduced. ADR-0002 byte-unchanged (`git diff --exit-code` = 0); index 1:1 with ADR files (TEMPLATE excluded); ADR-0001/0003 template-compliant + spec-traceable (Part 2/3/4, P1-WP04); status.md + rebuild README links resolve, stale ID-storage wording gone; `missing_links=0`, numbering monotonic; docs-only diff. Financial/security checks N/A. N1 (non-blocking, accepted): no-data-migration decision folded into ADR-0001 is a documented, spec-grounded refinement of the plan's Non-goal (no separate cutover ADR created). → done; cleared for LL Git. |
| 2026-07-06 | P1-WP05 | LL Git | Branch `P1-WP05-adr-log-foundational-adrs` + commit `8462e21` → PR #10 **squash-merged to main** (`89e6307`). Post-merge: synced main, pruned all merged remote + stale local branches (PRs #2/#5/#6/#8/#9/#10); only `main` remains. Phase 1 complete. |
| 2026-07-06 | Phase 2 | LL Architect | **Decomposed Phase 2 (Ledger core) into 8 WPs** (P2-WP01…08), fixtures-first. Researched specs (Part 3 schema/modules/API/security, Part 4 §Phase 2, Part 5 §1.1 testing pyramid, Part 6 M1 boundary) + old-code surface (`shared/postingValidity.ts`, `shared/periodUtils.ts`, `shared/fxPolicy.ts` with file/line refs via GitHub search). Drafted [P2-WP01 plan](./plans/P2-WP01-golden-fixtures-posting-rules.md) (golden fixtures — the porting oracle; LL Fixture Smith; 7 acceptance criteria; balance excluded → DB trigger + property suite in WP08). Folded risk-review N2/N3 → WP05, N4 → WP07. Two rule-interpretation questions (period-required?, currency-policy target) surfaced for LL Accounting Expert at WP04 planning. Recorded the WP breakdown in the Phase-2 tracker. Awaiting approval of P2-WP01. |
| 2026-07-06 | P2-WP01 | LL Fixture Smith | **Captured 59 golden fixtures** by executing the OLD impl (SHA `085bedba`, `TZ=UTC`) via committed harness `tools/fixtures/ledger-core/capture.test.ts` (copied into old repo `tests/`, run, temp deleted → old repo pristine). Set under `tests/fixtures/golden/ledger-core/` (posting-accounts 10, business-partners 7, users 7, projects 5, currency-policy 11, period-state 8, fx-metadata 11) + `manifest.json` (1:1, 0 orphans/dups), `SOURCE.md` (SHA + per-unit line refs), `fixture-format.md` (input/expected `ok｜error｜value` schema). All branches + boundaries covered; error shapes pinned (`PostingValidityError`/`CurrencyPolicyError`/`PeriodClosedError`); business/personal asymmetries pinned. Byte-identical re-capture verified (SHA-256 tree hash). Diff scope: only `tests/fixtures/**` + `tools/fixtures/**` + `status.md`. → verify. |
| 2026-07-06 | P2-WP01 | LL QA Reviewer | **PASS → done.** 7/7 ACs independently reproduced: manifest 1:1 (59/59, 0 orphans/dups); branch coverage complete incl. business/personal asymmetries + validFrom/validTo boundaries; 23 error cases with structured `issues[]`/`PeriodClosedError` payloads; provenance SHA `085bedba` + harness executes old impl (old repo left pristine on re-run); byte-identical re-capture (SHA-256 tree hash) + 0 datetime/timezone leaks across all 59; `fixture-format.md` documents `ok｜error｜value`; diff scope only `tests/fixtures/**`+`tools/fixtures/**`+`status.md`(+plan), CI gates (app/backend only) unaffected. Fidelity spot-check vs old source (boundaries, liability FX fallback, group/account override precedence, case-insensitive currency) all match. Hallucination scan: `{value}` variant required by AC2 + documented (accepted). Patch-layering: `serializeError` re-throws unexpected types (no masking). Security/financial: synthetic data, no secrets, no money amounts (balance → WP08). N1 (non-blocking): period-required?/currency-policy-target open questions routed to LL Accounting Expert at WP04. Cleared for LL Git. |
| 2026-07-06 | P2-WP01 | LL Git | Branch `P2-WP01-golden-fixtures-ledger-core` + commit `495592b` (67 files) → PR #11 **merged to main**. Added `.gitattributes` `eol=lf` pin for `tests/fixtures/golden/**` + `tools/fixtures/**` so byte-identical re-capture holds on Windows (`core.autocrlf=true`). |
| 2026-07-06 | P2-WP02 | LL Architect | Plan drafted: [P2-WP02](./plans/P2-WP02-postgres-schema-ef-migrations.md) — Postgres schema + EF migrations for the Phase-2 core (spaces/memberships/account_groups/accounts/periods/journal_entries/journal_lines/line_attributions/audit_log). **Schema/DDL only, no domain** (posting rules → WP04 vs P2-WP01 fixtures). Encodes Part 3 §4: uuid ids (ADR-0002), bigint minor units, RLS second wall (fail-closed, `app.current_space_id` GUC; request-time binding → WP06), **deferred balance constraint trigger** (`SUM(base_amount_minor)=0`, N3 DB half), append-only journal immutability, audit triggers, GiST code-range exclusion. 9 concrete ACs on the Testcontainers tier. Two deps recorded (Npgsql EF provider + EF Design). **Decision D1** surfaced (single `LedgerDbContext` baseline vs per-module split → candidate ADR-0004) — awaits user approval. No accounting consult / no golden fixtures required (no behavior ported). Awaiting approval. |
| 2026-07-10 | P2-WP02 | LL QA Reviewer | **QA FAIL → in-progress.** Reproduced local gates: build `-c Release` **0/0**; `--filter Category!=Integration` = arch **3/3** + unit **45/45** (AC1 met, `EfCoreIsConfinedToInfrastructure` green with the module added). Scope matches plan; DDL/config reviewed sound (bigint minor units, char(3) currency, uuid ids, deferred balance trigger, SECURITY-DEFINER audit, least-privilege grants, RLS force+policies, btree_gist uuid exclusion; no secrets; no patch-layering). **4 blocking findings** — explicitly-stated AC sub-clauses with no test (QA cannot assume untested behavior): F1 AC3 no-GUC fail-closed (`RlsTenancyTests`); F2 AC5 `reverses_entry_id` reversal-insert success (`ImmutabilityTests`); F3 AC6 DELETE-audit + journal-tables-produce-no-audit (`AuditTriggerTests`); F4 AC8 negative amount-typing (no real/double/money) + all `*_id` uuid (`SchemaMigrationTests`). Integration ACs 2–8 unrunnable locally (no Docker) → must be CI-green after fixes. → LL Backend Dev to add F1–F4. |
| 2026-07-10 | P2-WP02 | LL Backend Dev | **QA F1–F4 fixed → verify.** Installed **Docker Desktop 4.81** (winget, arm64) + started engine (`linux/aarch64`), so the integration tier ran locally (image `postgres:17`). Added 6 tests + 1 fixture helper: F1 `RlsTenancyTests.App_role_with_no_space_context_is_fail_closed` (non-pooled `OpenAppNoContextAsync` — Npgsql resets `SET ROLE` but not a `set_config()` GUC, so a pooled conn had leaked a prior space id and masked fail-closed); F2 `ImmutabilityTests.App_role_can_insert_a_reversal_entry_linked_via_reverses_entry_id`; F3 `AuditTriggerTests.Delete_writes_an_audit_row_...` + `Journal_tables_produce_no_audit_rows`; F4 `SchemaMigrationTests` no-float/money, fx_rate-only-numeric, every-`*_id`-uuid, + 9-table existence theory. **Results:** build `-c Release` **0/0**; full `dotnet test LeafLedger.sln` = **unit 45/45 + arch 3/3 + integration 37/37 green** (integration 21→37). No production DDL/config changed — pure test additions. → LL QA Reviewer (re-verify). |
| 2026-07-10 | P2-WP02 | LL QA Reviewer | **PASS (re-verify) → done.** Independently reproduced with Docker now local: build `-c Release` **0/0**; `dotnet test LeafLedger.sln` = **unit 45/45 + arch 3/3 + integration 37/37**. All 4 findings closed — re-ran each fix test by name (F1 fail-closed, F2 reversal-insert, F3 DELETE-audit + journal-no-audit, F4 no-float/money + fx_rate-only-numeric + every-`*_id`-uuid + 9× table-exists) all PASS. Scope confirmed pure test additions (tracked `src` diff = Host wiring only; production module byte-identical). **All 9 ACs now executed locally** (Docker), not just statically reviewed. Security/financial/patch-layering unchanged (sound); no secrets. **Non-blocking carry-forward N-WP06-pool:** WP06 RLS binding must set the space GUC per request (or txn-local) — pooled connections retain a `set_config(..,false)` GUC; recorded in plan Open questions. → LL Git. |
| 2026-07-11 | P2-WP02 | LL Git | Branch `P2-WP02-postgres-schema-ef-migrations` (base `origin/main` @ `631ceac`) + commit `ced3bfb` (39 files: `LeafLedger.Modules.Ledger` module + `InitialLedgerSchema` migration + 7 `Ledger/` integration tests + Host/arch/test wiring + plan + status; no bin/obj). Pushed; opened **draft PR #12** → https://github.com/Lokkeccs/LeafLedger/pull/12. Merge only on green CI. |
| 2026-07-11 | P2-WP02 | LL Git | User confirmed **PR #12 merged to main, all CI green** (`origin/main` = `32fc5a7`). P2-WP02 complete. |
| 2026-07-11 | P2-WP03 | LL Architect | Plan drafted: [P2-WP03](./plans/P2-WP03-chart-of-accounts-domain.md) — ≤2-day pure ChartOfAccounts domain port (accounts, inclusive group code-range value object, currency policy, FX-policy/metadata resolution) in a separate module; **22 existing P2-WP01 fixtures** are the executable oracle (11 currency + 11 FX). Salvage: `postingValidity.ts` currency unit + `fxPolicy.ts`; rewrite: typed C# domain/module. Explicit non-goals: EF/context ownership move, migration, CRUD/endpoints, built-in chart seeds/code suggestions, posting-validity/period rules, FX arithmetic/revaluation. 10 concrete ACs. **Blocked before implementation on LL Accounting Expert A1–A2:** approve/correct currency-policy target and FX matrix/override limits (esp. investment historical treatment). Awaiting expert answers + user approval. |
| 2026-07-11 | P2-WP03 | LL Accounting Expert | **A1 approved with guard:** P&L accounts may accept any transaction currency; balance-sheet/equity accounts use one fixed account currency. Empty currency remains a policy-evaluator no-op only; P2-WP05 must reject absent/invalid currency and translate every posting to balanced base minor units (OR 958d(3)). **A2 approved as defaults, not universal law:** monetary→closing revalue; historical→no routine closing revalue; observable-market current value where consistently elected; account override > group override > inference. **Investment historical treatment may be overridden** for justified monetary instruments or an OR 960b observable-market valuation policy; participations/non-monetary investments remain historical-cost/impairment by default (OR 960a). Do not port the unpinned UI force helper. Evidence/consistency/impairment/reserve/disclosure enforcement carries to account-maintenance + FX-revaluation WPs. No fixture change required. Accounting blocker cleared; awaiting user approval. |
| 2026-07-11 | P2-WP03 | LL Backend Dev | User approved plan after A1–A2 sign-off. Created branch `P2-WP03-chart-of-accounts-domain` from merged `origin/main` (`32fc5a7`). State → in-progress; fixture-first implementation started. |
| 2026-07-11 | P2-WP03 | LL Backend Dev | **Implemented → verify.** TDD red baseline first (missing module); then pure `LeafLedger.Modules.ChartOfAccounts` (SharedKernel-only, 0 packages): Account/Group/range models, currency evaluator, FX inference + group/account overrides + `DateOnly` metadata. Fixture loader content-links P2-WP01 artifacts and guards exact selection/unknown units/duplicates/missing files. **Results:** golden **22/22** + structural/loader **11/11**; Release build **0/0**; full Docker suite **118/118** (SharedKernel 45 + CoA 33 + arch 3 + integration 37, including no-pending-model-changes). Architecture includes new module; Domain explicitly cannot depend on Ledger/EF/AspNetCore. Scope clean: no persistence/schema/Host/API/frontend/workflow edits. → LL QA Reviewer. |
| 2026-07-11 | P2-WP03 | LL QA Reviewer | **PASS → done.** Independently reproduced from a clean `--no-incremental` rebuild: Release **0/0**; `Category!=Integration` = SharedKernel 45 + CoA 33 + arch 3 = **81/81**; `Category=Integration` (Docker `postgres:17`) = **37/37** incl. `HasPendingModelChanges()` → no Ledger drift; **total 118/118**. CoA split verified: golden **22/22** + structural **11/11**. All 10 ACs met with specific evidence. Diff scope = sln + arch csproj/test + status + new module/tests only (no `app/**`, `openapi/**`, Ledger `Infrastructure/**`, migration, Host, workflow). Financial: no float/decimal, no amounts, `Account` requires valid `CurrencyCode`. Hallucination scan: unpinned `shouldForceHistoricalForInvestmentGroup` absent (A2 honored). Patch-layering: no prod try/catch; loader throws specific `InvalidDataException`/`FileNotFoundException`. Non-blocking carry-forwards (currency command guard, FX evidence/impairment/OR-960b, int4range boundary conversion) already recorded in plan. → **done**; cleared for LL Git. |
| 2026-07-11 | P2-WP03 | LL Git | Branch `P2-WP03-chart-of-accounts-domain` (base `origin/main` @ `32fc5a7`) + commit `46b8b2f` (24 files: `LeafLedger.Modules.ChartOfAccounts` module + tests + sln/arch wiring + plan + status; no bin/obj). Pushed; opened **draft PR #13** → https://github.com/Lokkeccs/LeafLedger/pull/13. Merge only on green CI. |
| 2026-07-11 | P2-WP03 | LL Git | User confirmed **PR #13 merged to main, all CI green**. P2-WP03 complete. Phase 2 progress: WP01–WP03 done; next is P2-WP04 (Ledger domain: JournalEntry aggregate). |
| 2026-07-11 | P2-WP04 | LL Git | Branch `P2-WP04-ledger-domain` + commit `703a8b6` (29 files: Ledger.Domain + tests + ADR-0005 + solution/architecture wiring + plan + status; no bin/obj). Draft PR **#14** was opened and user confirmed it merged to `main` with all CI green. P2-WP04 complete. Phase 2 progress: WP01–WP04 done; next is P2-WP05 (posting/reversal/period endpoints + idempotency N2 + eager balance check N3). |
| 2026-07-11 | P2-WP05 | LL Architect | Plan drafted → state `planned`, and **re-scoped Phase 2**: the original WP05 bundle exceeded the ≤2-day rule, so it is split into **WP05** (posting/reversal application + two write endpoints), **WP09** (idempotency N2 — new `idempotency_keys` migration, replay, 24h TTL, collision metric), and **WP10** (period-lifecycle create/open/close/lock + onboarding open-period). [P2-WP05 plan](./plans/P2-WP05-posting-reversal-endpoints.md): composes WP03 `CurrencyPolicyEvaluator` + WP04 `PostingValidityEvaluator`/`PeriodStateResolver`/`JournalEntry` into `POST /journal-entries` (+`/reverse`); eager balance 422 **before COMMIT** (N3 app half) with the WP02 deferred trigger as the independent DB second wall; structured 422 ProblemDetails with standardized codes; **txn-local** RLS binding (`SET LOCAL ROLE`+`is_local=>true`, resolving carry-forward N-WP06-pool); per-space `entry_no` allocation. Verified against real schema (no `idempotency_keys`/partner/project/VAT tables → those checks deferred; `periods.end_exclusive` vs domain inclusive `EndDate` off-by-one flagged). Non-goals: authz (WP06), idempotency (WP09), period writes (WP10), reporting (WP07), FX arithmetic. Routed **B1** (M1 base-currency handling — client-supplied+validated vs single-currency-only), **B2** (period overlap/gap policy, for WP10), **B3** (`entry_no` gapless vs monotonic) to LL Accounting Expert; implementation blocked until B1+B3 + user approval. WP08 exit-gate deps extended to include WP09/WP10 (exactly-once + closed-period). |
| 2026-07-11 | P2-WP05 | LL Accounting Expert | **B1 resolved:** accept multi-currency M1 postings with client-supplied `base_amount_minor` and `fx_rate`, strictly server-validated; same-currency base equals transaction amount; foreign amounts require positive rate, matching sign, deterministic `AwayFromZero` conversion consistency within one base minor unit, and balanced base sum; persist rate/base as immutable evidence; no lookup/revaluation in WP05. **B3 resolved:** monotonic, unique, never-reused `entry_no` per space overall; rollback gaps are permitted but must remain explainable; no fiscal-year partitioning; allocation must be concurrency-safe and must not use `MAX(entry_no)+1`. Required consequence: LL Fixture Smith captures focused FX/base-amount rounding fixtures before WP05 implementation. B2 remains with WP10. |
| 2026-07-11 | P2-WP05 | LL Fixture Smith | **UNVERIFIED fixture package added:** inspected OLD SHA `085bedba`; no posting-time base-amount conversion/validation function exists, so no OLD outputs could be captured. Added 9 canonical B1 cases under `tests/fixtures/golden/ledger-posting-base/`, each marked `UNVERIFIED` in the manifest and expected block; no hand-authored oracle values. The OLD checkout had unrelated pre-existing generated-file changes; this task added no files or changes there. LL Backend Dev must promote these to target-decision assertions after implementing the validator; they must not be labeled OLD golden matches. |
| 2026-07-11 | P2-WP05 | LL Backend Dev | Implementation started; focused B1 validator tests added first. |
| 2026-07-11 | P2-WP05 | LL Backend Dev | Implemented application posting/reversal, B1 exact-rational validation, txn-local RLS binding, per-space entry numbering, Host endpoints, OpenAPI/TS regeneration, and focused PostgreSQL coverage. Results: Release build 0/0; backend non-integration 143/143; integration 42/42; frontend tests 3/3; lint/typecheck/page-budget/build green. State remains `in-progress`: HTTP/TestServer endpoint and ProblemDetails acceptance coverage is the one concrete follow-up. |
| 2026-07-11 | P2-WP05 | LL Backend Dev | Added real `WebApplicationFactory<Program>`/TestServer coverage using the existing PostgreSQL fixture and seeds: **4/4 HTTP tests green** (201 post + Location/body, 422 ProblemDetails with stable code/errors/issues, reversal 201 + link, missing target 404, actor 400s, GET route 405). Added test-only `Microsoft.AspNetCore.Mvc.Testing`/`Microsoft.AspNetCore.TestHost` 9.0.0 and Host reference; no Domain, migration, authz, idempotency, or period-lifecycle changes. Results: Release build 0/0; architecture 3/3; Release integration 46/46; frontend test 3/3, lint/typecheck/page-budget/build green. Contract regeneration is clean relative to the expected WP05 additions, but the workspace retains those uncommitted generated OpenAPI/TS additions, so the raw git diff gate reports drift. State → `verify`; next LL QA Reviewer reproduction. |
| 2026-07-11 | P2-WP05 | LL Backend Dev | Fixed two QA findings: `ReverseWithinTransactionAsync` now loads/persists `LineAttributions` and uses `JournalEntry.Reverse(...)`, so `long.MinValue` reversal returns structured `journal_entry.amount_out_of_range` instead of throwing. Added real PostgreSQL attribution-preservation coverage and real HTTP/TestServer extreme-amount coverage; focused regressions **2/2** and existing posting/reversal integration tests **5/5** pass in Release. State remains `verify`; next action is full independent QA reproduction. |
| 2026-07-11 | P2-WP05 | LL QA Reviewer | **QA PASS -> done.** Independently reproduced the Release build, architecture/boundary tests, backend unit and PostgreSQL integration suites, HTTP/TestServer endpoint coverage, frontend gates, and contract generation. All 12 acceptance criteria pass; WP05 is complete. Nine canonical B1 cases remain explicitly UNVERIFIED because the OLD repository has no executable base-amount validator. Production exposure remains gated by WP06 authorization and WP09 idempotency. |
| 2026-07-11 | P2-WP05 | LL Git | PR **#15** merged to `main` with all CI green. WP05 is complete on the default branch; next planned work is P2-WP06 authorization. |
| 2026-07-11 | P2-WP06 | LL Architect | Plan drafted → state `planned`, and **re-scoped** the WP06 bundle. Researched spec §8 (AuthZ pipeline scope→license→role→permission; RLS second wall), §2 verdicts (client-side rights table + HS256 device-license both **Replace**), §3/§5 (Spaces/Licensing module boundaries; Host owns middleware), and the OLD code (`src/auth/rights.ts`/`AuthContext.tsx`/`RolesPage.tsx` = client-side authZ; `CosmosLicenseService`/`functions/license` = device HS256 JWT) — confirmed **spec-derived rewrite, no salvage, no golden fixtures**. The literal bundle spans three absent module contexts (Licensing entitlements, identity-links, full Entra validation) → exceeds ≤2 days, so [P2-WP06](./plans/P2-WP06-authorization-pipeline.md) is scoped to the authorization core (typed `SpaceRole` → `ledger.post`/`ledger.reverse` endpoint filter on the two WP05 endpoints; 401/403 ProblemDetails; principal-bound txn-local RLS binding replacing `X-Actor-Id`), with authN as a configured JWT-bearer + test-scheme seam and license as a called `ILicenseEntitlement` allow-all seam. Carved out **P2-WP11** (AuthN/Entra + identity-links) and routed real license enforcement to the **Licensing module**. 12 concrete ACs on the per-endpoint HTTP + RLS tier. No accounting consult (access control). Awaiting user approval. |
| 2026-07-11 | P2-WP06 | LL Architect | Plan drafted → state `planned`, and **re-scoped** the WP06 bundle. Researched spec §8 (AuthZ pipeline scope→license→role→permission; RLS second wall), §2 verdicts (client-side rights table + HS256 device-license both **Replace**), §3/§5 (Spaces/Licensing module boundaries; Host owns middleware), and the OLD code (`src/auth/rights.ts`/`AuthContext.tsx`/`RolesPage.tsx` = client-side authZ; `CosmosLicenseService`/`functions/license` = device HS256 JWT) — confirmed **spec-derived rewrite, no salvage, no golden fixtures**. The literal bundle spans three absent module contexts (Licensing entitlements, identity-links, full Entra validation) → exceeds ≤2 days, so [P2-WP06](./plans/P2-WP06-authorization-pipeline.md) is scoped to the authorization core (typed `SpaceRole` → `ledger.post`/`ledger.reverse` endpoint filter on the two WP05 endpoints; 401/403 ProblemDetails; principal-bound txn-local RLS binding replacing `X-Actor-Id`), with authN as a configured JWT-bearer + test-scheme seam and license as a called `ILicenseEntitlement` allow-all seam. Carved out **P2-WP11** (AuthN/Entra + identity-links) and routed real license enforcement to the **Licensing module**. 12 concrete ACs on the per-endpoint HTTP + RLS tier. No accounting consult (access control). Awaiting user approval. |
| 2026-07-11 | P2-WP06 | LL Backend Dev | User approved implementation. Added Host JWT/test authentication seams, typed roles and permissions, ordered license/membership/permission endpoint filters, principal-bound actor/RLS flow, membership query, and protected WP05 endpoints. Regenerated OpenAPI/TS artifacts. **Validation:** Release backend **227/227**, architecture **3/3**, integration **84/84**, focused authorization **36/36**, frontend tests/lint/typecheck/page-budget/build green; no migration or Domain changes. State → `verify`; next LL QA Reviewer. |
| 2026-07-11 | P2-WP06 | LL QA Reviewer | **PASS → done.** Independently reproduced local checks: Release build `0/0`, backend `227/227` (arch `3/3`, PostgreSQL integration `84/84`), frontend `3/3`, lint/typecheck/page-budget/build green. Addressed QA findings prior to sign-off: enforced required authorization callback on `MapLedgerEndpoints`, unified identity reading via `AuthorizationContext`, corrected literal placeholder issuer in `appsettings.json`, and corrected test count to `41/41`. Golden fidelity N/A. Architecture boundaries pristine (EF isolated in Infrastructure). Security checks pass. Cleared for LL Git. |
| 2026-07-11 | P2-WP07 | LL Architect | Plan drafted → state `planned`, and **re-scoped** the WP07 bundle. Researched spec §4.3 (reports as SQL views + `/integrity` trial-balance hash), §4.2 (schema: signed `base_amount_minor`, `accounts.kind`, posted status), §6 (report/integrity API), §72 (SQL reporting = **new capability**, not a port), §8.3 (RLS second wall), Part 5 (`/integrity` deploy probe), N4. Confirmed via GitHub search that OLD reporting was **UI-computed over Dexie** with no capturable pure oracle → **spec-derived rewrite, no golden fixtures** (pinned by integration + determinism unit + WP08 ≡0). The literal bundle (3 views + integrity + full N4 mat-view refresh machinery) exceeds ≤2 days, so [P2-WP07](./plans/P2-WP07-reporting-views-integrity.md) is scoped to the **correctness half**: three plain `security_invoker` SQL views (`trial_balance`/`balance_sheet_lines`/`income_statement_lines`, always live), deterministic SHA-256 trial-balance integrity hash + `balanced` flag, four `ledger.read`-gated `GET` endpoints (new permission granted to all roles incl. Viewer), txn-local RLS-bound reads. Carved **N4 materialization** into new **P2-WP12** (proposed; folded N4 there). Routed one accounting gate **C1** (balance-sheet/income-statement presentation sign convention + net-result line, per OR 959/959a/959b) to LL Accounting Expert; trial-balance ≡0 + integrity hash need no consult. 12 concrete ACs on the view/integrity/RLS/authorization tier; `security_invoker=true` flagged as load-bearing for tenancy. `vat_report` deferred to Phase 4; report UI to Phase 3/M1. Implementation blocked until C1 + user approval. |
| 2026-07-11 | P2-WP07 | LL Accounting Expert | **C1 resolved.** Trial balance remains raw debit-positive / credit-negative. Balance-sheet presentation keeps Assets as-is and flips Liabilities/Equity; income-statement presentation flips Income and keeps Expenses positive. Add one derived `net_result = total_income - total_expenses` line; surface current result once in equity without double-counting. Decision is presentation-level and target-derived under OR 959/959a/959b; pin with explicit-value integration tests, not OLD golden fixtures. |
| 2026-07-11 | P2-WP07 | LL Backend Dev | **Implementation in progress.** Added `ReportingViews` raw-SQL migration (`security_invoker` plain views + grants), application report contracts, transaction-local `LedgerReportService`, `ledger.read` role mapping, four authorized GET routes, and OpenAPI bearer coverage for all space routes. `IntegrityHasher` + 4 focused determinism tests are green; Release Host build is green. PostgreSQL integration, no-model-drift, full contract regeneration, and final acceptance tests remain. |
| 2026-07-11 | P2-WP07 | LL Backend Dev | **Validation update:** fixed EF discovery metadata for the hand-authored reporting migration after the first HTTP smoke test found views absent; focused report HTTP test now passes 1/1. Full backend non-integration/architecture 170/170, PostgreSQL integration 85/85, frontend lint/typecheck/tests/page-budget/build green, and OpenAPI + TS schema regenerated. WP07 remains `in-progress` pending explicit C1 values and the full authorization/RLS/no-drift matrix. |
| 2026-07-11 | P2-WP07 | LL Backend Dev | **Implemented → verify.** Moved C1 sign transformations into the SQL presentation views; added explicit seeded values for all five account kinds, net/current result, unbalanced integrity, stable ProblemDetails codes, cross-space denial, and no-context RLS. Final solution-wide Release validation: **236/236**; frontend lint/typecheck/tests/page-budget/build green; OpenAPI/TS regenerated; no-model-drift test green. Next: LL QA Reviewer. |
| 2026-07-11 | P2-WP07 | LL Backend Dev | **QA finding fixed.** Added typed `200` response metadata to all four report/integrity endpoints and regenerated OpenAPI/TS artifacts. Contract now includes the four response schemas; Host Release build, frontend typecheck/tests **3/3**, and report integration tests **5/5** pass. State remains `verify`; next: LL QA Reviewer. |
| 2026-07-12 | P2-WP07 | LL Git | User confirmed **PR #17 merged to main**. P2-WP07 complete. Phase 2 progress: WP01–WP07 done; next planned work is P2-WP08 (financial-invariant property suite) / P2-WP12 (mat-view refresh strategy) depending on scheduling. |
| 2026-07-12 | P2-WP08 | LL Architect | Plan drafted → state `planned` but **BLOCKED on WP09 + WP10**. [P2-WP08 plan](./plans/P2-WP08-financial-invariant-property-suite.md): the flagship **PR-blocking** financial-invariant property suite (Phase-2 exit gate). Grounded in Part 4 §Phase 2 exit criterion + Part 5 §1.1/§1.2 (invariant suite on the PR pipeline). Seven invariants I1–I7: balance ≡ 0 for any generated post/reverse sequence (WP07 `trial_balance`/`integrity` oracle), unbalanced/closed-period/invalid-ref → 422 (WP05 codes), retried posts exactly-once (**WP09**), DB balance-trigger + RLS direct second wall (WP02), period-boundary inclusivity (WP04/WP05 carry-forward, P1-WP03 half-open `[start,end_exclusive)`). Model-based property testing (bounded generator + reference model + mandatory shrinking/seeded replay) over the real HTTP+`postgres:17` stack, reusing WP05/06/07 `WebApplicationFactory`/`LedgerDbFixture`/`TestAuthHandler`/seed helpers. **Decisions:** D-WP08-GEN engine = CsCheck test-only NuGet (shrinking) with a generative-xUnit fallback that must still log seeds + minimize; D-WP08-CI a blocking `Category=Property` PR step (Docker), unit step filter → `Category!=Integration&Category!=Property`. **No golden fixtures** (oracle is the ≡0 invariant + reference model + already-captured WP01 rule fixtures) and **no accounting consult** (mechanical invariants; I7 reuses the P1-WP03 half-open rule; a real boundary off-by-one is a routed finding, not a WP08 fix). Test+CI+docs only — no production `src/**`/migration/DDL/`app/**` change. FX/VAT rounding + load/perf deferred. **Correct sequence WP09 → WP10 → WP08**; drafted now so WP09/WP10 acceptance criteria can name the WP08 I5/I3 consumers. Split seam (WP08a/WP08b) documented if the whole exceeds ≤2 days. Awaiting user approval + the two blockers. |
| 2026-07-12 | P2-WP09 | LL Architect | Plan drafted → state `planned` (unblocked; implementable against current `main`). [P2-WP09 plan](./plans/P2-WP09-idempotency-middleware.md): idempotency middleware (**N2**) filling the seam WP05 reserved — the `Idempotency-Key` header is already parsed and `JournalPostingService.ExecuteAsync` already takes an unused `idempotencyKey`. Grounded in Part 3 §"Posting"/§6 (client-ULID-per-mutation, unique-indexed, exactly-once), Part 5 §1.1/§67, playbook §88/§204 (non-negotiable #4), N2 (24h TTL + cleanup + collision metric), weaknesses §1.5 (OLD push was non-idempotent → duplicates manually purged; motivation, not a port). Scope: raw-SQL `idempotency_keys` table (`UNIQUE(space_id,key)` + RLS tenant policy + SELECT/INSERT grants, **no EF entity** → `HasPendingModelChanges()` stays green, WP07 precedent); enforce/replay/store **atomically inside the posting transaction** (D-WP09-LAYER, not a pre-handler HTTP middleware — atomicity is the crux); retried same key+payload → replay (one `entry_no`, `Idempotent-Replayed: true`); same key+different payload → **409** `idempotency.key_reused` + `leafledger.idempotency.collisions` BCL counter; 24h TTL cross-tenant cleanup `BackgroundService` on the owner connection (D-WP09-CLEANUP); **header required** on writes (D-WP09-REQ, 400 `idempotency.key_required`/`key_invalid` — tightens the WP05 contract + tests, safe pre-production); ULID stored as uuid via the N1 converter (D-WP09-KEY); canonical SHA-256 request hash for collision detection (D-WP09-HASH). **No golden fixtures / no accounting consult** (transport mechanism; pinned by integration tests + the WP08 I5 property + a hash-determinism unit test). No new production NuGet (SHA-256/Metrics/BackgroundService are BCL). **Independent of WP10; sequence WP09 → WP10 → WP08.** 12 concrete ACs incl. atomicity (AC7), concurrency exactly-once (AC6), and the keys RLS second wall (AC8). Awaiting user approval. |
| 2026-07-12 | P2-WP09 | LL Backend Dev | Implementation started from the user request: migration/store/hash, atomic transaction enforcement/replay/collision/expiry, required headers, cleanup, metric, OpenAPI/TS regeneration, and focused unit/integration coverage added. Release backend, non-integration backend **172/172**, architecture, hash **2/2**, and frontend gates pass. Docker-backed integration tests are blocked by the local Testcontainers Docker endpoint; state remains `in-progress` pending Docker execution and full acceptance reproduction. |
| 2026-07-12 | P2-WP09 | LL Backend Dev | Fixed replay response fidelity after Docker exposed JSONB camelCase deserialization loss; focused replay test **1/1** and complete Release backend suite **242/242** now pass (SharedKernel 45, ChartOfAccounts 33, Ledger 68, architecture 3, integration 93). State → `verify`; next LL QA Reviewer. |
| 2026-07-12 | P2-WP09 | LL Backend Dev | Addressed QA findings: cross-tenant cleanup now invokes a security-definer purge function instead of direct table deletion; cleanup execution through `leafledger_app` passes. Added reverse replay, expiry reuse, concurrent duplicate, failed-post atomicity, and cross-space isolation tests. Full Release suite **247/247** green (integration **98/98**). State remains `verify`; next LL QA Reviewer. |
| 2026-07-12 | P2-WP09 | LL Git | PR #18 merged to `main` as `fb1969c` (`P2-WP09: Idempotency middleware (N2)`). WP09 → **done**; WP08 I5 unblocked. Next planned work: WP10, then WP08. |
| 2026-07-12 | P2-WP10 | LL Backend Dev | Implemented period lifecycle, half-open boundary reconciliation, overlap exclusion, bootstrap helper, authorization/idempotency wiring, contract regeneration, and fixture inventory update. Release backend **264/264**; focused period integration **3/3**; frontend lint/typecheck/tests **3/3**/page-budget/build/contract generation green. State → **verify**; next LL QA Reviewer acceptance reproduction. |
| 2026-07-12 | P2-WP10 | LL Backend Dev | Fixed QA findings: EF-wrapped PostgreSQL exclusion violations now return structured `period.overlap` 409; added period authorization/cross-space integration coverage (**6/6**); made exact fiscal-year bootstrap safe to invoke repeatedly and added a `[start, end_exclusive)` regression. Full Release backend suite **271/271**. Explicit posting boundary and closed/locked/gap acceptance coverage remains; state stays **verify**. |
| 2026-07-12 | P2-WP10 | LL Backend Dev | Completed the remaining acceptance coverage: lifecycle-created periods accept the last inclusive day and reject `endExclusive`; undefined gaps return `posting_period.not_defined`; closed and locked periods return `posting_period.not_open`; bootstrap-created periods accept an in-range post. Focused lifecycle suite **9/9**; full Release backend suite **276/276**. State remains **verify** pending QA sign-off. |
| 2026-07-12 | P2-WP10 | LL Accounting Expert | **B2.1–B2.5 resolved.** Per-space date overlaps are prohibited and enforced by application validation plus the GiST exclusion second wall; gaps are permitted and undefined-date postings reject as `posting_period.not_defined`; M1 accepts arbitrary non-empty half-open ranges and defers bulk fiscal generation; the OLD transition matrix remains authoritative, with privileged Owner/Admin lock exposed as a documented target-surface expansion and `Locked` terminal; onboarding bootstraps one transactional/idempotent current-fiscal-year open period with configured fiscal-year start month, defaulting to January. Full-year bootstrap ranges are canonical for that year and cannot later be subdivided without a future non-overlapping setup flow. B2 blocker cleared; transition/boundary fixtures and user approval remain. Next: LL Fixture Smith. |
| 2026-07-12 | P2-WP10 | LL Fixture Smith | Captured the OLD transition and boundary oracle at SHA `085bedba`: all 9 `updatePeriodState` state-pair cells plus start/inclusive-end/after-end `getPeriodForDate` cases. Added 12 fixtures under `tests/fixtures/golden/ledger-core/period-lifecycle/`, unit/root manifests, `SOURCE.md`, and a committed capture harness. OLD run passed **12/12**; recapture was byte-identical; temporary OLD test was removed. Fixture gate cleared. Next: user approval, then LL Backend Dev. |
| 2026-07-12 | P2-WP08 | LL Backend Dev | Continued implementation on merged WP09/WP10 baseline. Added the HTTP/Testcontainers driver and implemented I1 zero-balance/replay, I2 unbalanced rejection, I3 undefined/closed-period rejection, I4 missing-reversal rejection, and I5 idempotency collision properties. Focused Docker-backed property suite **8/8** green. Next: I6 direct database second-wall properties and I7 explicit boundary property. |
| 2026-07-12 | P2-WP08 | LL Backend Dev | Added I6 direct deferred-trigger/RLS properties and I7 exact half-open period-boundary properties. Focused Docker-backed property suite **11/11** green; no production behavior changes. Next: Release build and full backend/architecture/unit acceptance gates. |
| 2026-07-12 | P2-WP08 | LL Backend Dev | Final local acceptance: Release build clean; full backend suite **287/287** (integration **124/124**); property suite **11/11**; temporary I1 mutation failed as expected, then was restored and revalidated. CI wiring and test-only scope verified. State -> **verify**; next LL QA Reviewer. |
| 2026-07-12 | P2-WP08 | LL QA Reviewer | **QA FAIL → in-progress.** Release build and executable gates reproduced: property **11/11**, full backend **287/287** (integration **124/124**). Four blocking coverage findings recorded in the plan: I1 generator omits distinct-account reversals and bypasses the HTTP/model property path; I3/I4 omit locked and invalid/cross-space/already-reversed cases; I6 does not assert cross-space reads; I7 and shrinking/replay are example-only or disconnected from HTTP/DB properties. |
| 2026-07-12 | P2-WP08 | LL Backend Dev | Fixed all four QA findings in the test tier. I1 now drives distinct-account posts/reversals and the reference model; I3/I4 add locked, invalid/cross-space, and already-reversed cases; I6 verifies hidden cross-space reads; I7 uses seeded async HTTP replay/shrinking; nondeterministic keys removed. Validation: property **14/14**; full backend **290/290** (integration **127/127**); Release build clean. State -> verify; next LL QA Reviewer. |
| 2026-07-12 | P2-WP08 | LL QA Reviewer | **QA re-review FAIL -> in-progress.** Reproduced Release build clean, property **14/14**, full backend **290/290** (integration **127/127**, architecture **3/3**). Three findings recorded in the plan: the reference model is not compared to per-account/system state, cross-space reversal targets are untested, and `LedgerDbFixture.cs` falls outside the plan's declared file list. |
| 2026-07-12 | P2-WP08 | LL Backend Dev | Fixed the QA re-review findings in the test tier. I1 now compares cumulative model balances and live source-entry counts with observable PostgreSQL/API state after every generated command, with deterministic post-key reversal mapping; I4 adds a real cross-space reverse-target rejection and unchanged-state assertions; the additional-account helper moved into `LedgerSystemDriver.cs`, so the fixture is no longer modified. Validation: focused property **15/15**; full Release backend **291/291**; `git diff --check` clean. State -> verify; next LL QA Reviewer. |
| 2026-07-12 | P2-WP08 | LL QA Reviewer | **QA PASS → done.** Independently reproduced focused property **15/15**, full Release backend **291/291** (integration **127/127**, architecture **3/3**), deterministic replay/shrinking, model/system agreement, cross-space reverse isolation, direct balance-trigger/RLS properties, and clean declared scope. Phase 2 exit criterion met. |
| 2026-07-12 | P2-WP08 | LL Git | **PR #21 merged to `main` as `1aa35ae`.** WP08 and Phase 2 are complete; next phase is P3 frontend re-platform. |
| 2026-07-12 | P2-WP11 | LL QA Reviewer | **QA FAIL → in-progress.** Release build `0/0`; focused authorization/JWT `35/35`; architecture `3/3`; full Release backend `292/292` (`129` integration) all pass. Findings: issuer parser accepts query/fragment components; v1 issuer and configured tenant-allowlist behavior are not proven through the real pipeline. |
| 2026-07-12 | P2-WP11 | LL Backend Dev | Fixed both QA findings: exact issuer parsing plus real JwtBearer v1/malformed/missing/tenant-allowlist coverage. Focused **41/41**; full Release backend **298/298** (`135` integration); architecture **3/3**; `git diff --check` clean. State → verify. |
| 2026-07-12 | P2-WP11 | LL QA Reviewer | **QA re-review PASS → verify.** Both findings closed. Unrelated pre-existing WP08 documentation changes remain in the working tree and must be separated before commit. |
| 2026-07-12 | P2-WP11 | LL QA Reviewer | **Independent re-review PASS → verify.** Focused **41/41**; Release build **0/0**; full backend **298/298** (`135` integration, `3` architecture); frontend lint/typecheck/tests/page-budget/build green; no OpenAPI/TS drift. No new findings. WP08 documentation remains outside WP11 scope and must be separated before commit. |
| 2026-07-12 | P2-WP11 | LL Git | **PR #22 merged to `main` as `20a8cc0`.** Resolved the shared WP08 plan/status documentation conflicts while preserving WP08's completed record and WP11's QA history. WP11 is complete; next phase is P3 frontend re-platform, with P2-WP12 and P2-WP13 remaining proposed follow-ups. |
| 2026-07-12 | P2-WP11 | LL Architect | Plan drafted → state `planned`, and **re-scoped** the WP11 bundle. Researched \u00a78.1 (AuthN: Entra `common`, audience + signature + issuer-pattern allowlist; `sessionStorage`/sign-out), weakness \u00a72.6 (`ValidateIssuer=false` + hardcoded audience + no issuer allowlist), \u00a72.5 + verdict line 51 (candidate-guessing/auto-join → identity-links), and the OLD code (`AuthServiceExtensions.cs` `ValidateIssuer=false`; `ClaimsPrincipalExtensions` `TryGetOid`/`GetTid`/`GetIdentityCandidates` fuzzy matching) — confirmed **spec-derived rewrite, no salvage, no golden fixtures**. The literal bundle (token validation **+** identity-links migration/resolver **+** sign-out) exceeds \u22642 days, so [P2-WP11](./plans/P2-WP11-authn-token-validation.md) is scoped to the **token-validation core**: `EntraIssuerValidator` (v1/v2 issuer shapes, `tid`-bound, optional `TenantAllowlist`) replacing the placeholder static `ValidIssuers`; audience fail-closed guard; JWKS-signature proof via a locally-minted RSA key (no live IdP); `tid` exposed on `ICurrentUser`. Verified against real Host wiring (`AuthenticationConfiguration`/`Program.cs`/`appsettings.json` `Authority=common/v2.0`) — **no migration, no endpoint/contract change** (bearer scheme already in the WP06 OpenAPI). D-WP11-ISSUER: hand-rolled validator (BCL + `Microsoft.IdentityModel.Tokens`, no new prod package) default; `Microsoft.IdentityModel.Validators` `AadIssuerValidator` documented fallback. Carved **P2-WP13** (identity-links `(sub/oid,tid)→user_id` + resolver + RLS-actor rewiring; new migration) and routed the frontend MSAL/`sessionStorage`/sign-out glue to Phase 3. No accounting consult (access control). 12 concrete ACs on the accept/reject matrix (signature/audience/issuer/`tid`/lifetime/tenant-allowlist) + WP06-outcome preservation + no-contract-drift. Awaiting user approval. |
| 2026-07-12 | P2-WP12 | LL Architect | Plan drafted → state `proposed`→`planned`. [P2-WP12 plan](./plans/P2-WP12-materialized-view-refresh.md): materialized-view refresh strategy (**N4**) for the hot reporting views, carved out of the `done` WP07. Researched §4.3 ("materialized where hot, refreshed on posting"), the N4 risk note (verbatim acceptance: `REFRESH … CONCURRENTLY` + unique index, post-commit enqueue, coalescing, refresh-duration/staleness metrics), and the real WP07/WP09 code (plain `security_invoker` views + txn-local GUC read binding; `BackgroundService`/`PeriodicTimer`; raw-SQL `SECURITY DEFINER` migration; `IMeterFactory` counter) — confirmed **spec+N4-derived performance rewrite, no OLD oracle, no golden fixtures, no accounting consult**. Scope: materialize the single hot base aggregation (`trial_balance_mat` + `UNIQUE(space_id,account_id)` → `REFRESH … CONCURRENTLY`), a per-space-coalescing post-commit `RefreshCoalescingService` (`BackgroundService`, enqueued **after** `CommitAsync`, never in the txn), duration/staleness/rows metrics. **Two load-bearing decisions front-loaded for user sign-off (not accounting):** **D-WP12-TENANCY** — PostgreSQL matviews **do not honor RLS**, so the WP07 `security_invoker` second wall breaks on materialization → mitigated by a GUC-predicate `security_invoker` **wrapper** view with `trial_balance_mat` **ungranted** (fail-closed; AC7 re-proof); candidate **ADR-0006**. **D-WP12-INTEGRITY** — integrity hash + WP08 ≡0 oracle + deploy `/integrity` probe stay **exact** via a separate always-live `trial_balance_live` view, dashboards read the eventually-consistent materialized path. Verified against real code (post commit `JournalPostingService` L105; `LedgerReportService`; `LedgerModule` wiring). **No new endpoints/shapes → OpenAPI/TS must regenerate byte-identical**; no new NuGet; `HasPendingModelChanges()` stays false. 12 concrete ACs; split seam WP12a (materialize+tenancy)/WP12b (coalescing+metrics) documented. **Blocked on D-WP12-TENANCY + D-WP12-INTEGRITY + user approval.** |
| 2026-07-12 | P2-WP12 | LL Architect | **Both decisions approved by the user** on their recommended routes → plan **unblocked, ready for implementation**. **D-WP12-TENANCY:** wrapper view + GUC predicate + `trial_balance_mat` ungranted (only safe way to preserve tenant isolation; avoids cross-tenant leaks) — **to be recorded as ADR-0006** by LL Docs Editor as the first implementation task. **D-WP12-INTEGRITY:** `/integrity` + WP08 ≡0 oracle read a separate always-live `trial_balance_live` view (keeps the deploy probe exact, avoids synchronous-refresh blocking); dashboards read the eventually-consistent materialized path. Plan §decisions/§open-questions/§sequence/§file-list updated to RESOLVED; status row `planned` (unblocked). Next: LL Docs Editor authors ADR-0006, then LL Backend Dev implements. |
| 2026-07-12 | P2-WP11 | LL Git | **PR #23 merged to `main` as `a660381`.** WP11 documentation status is finalized; WP11 implementation remains merged via PR #22 as `20a8cc0`. |
| 2026-07-12 | P2-WP12 | LL Git | **PR #24 merged to `main` as `2b315fa`.** WP12 materialized-view refresh implementation and documentation are complete; Phase 2 deferred follow-up is done. |
| 2026-07-12 | P2-WP13 | LL Architect | Plan drafted → state `proposed`→`planned` (awaiting user approval). [P2-WP13 plan](./plans/P2-WP13-identity-links.md): identity-links indirection carved out of the WP11 bundle. Researched §2 verdict line 51 (*"identity links table replaces runtime candidate guessing"*), §3 (Spaces & Identity owns identity links), §8.1 (*"kills the stale-space auto-join class"*), weakness §2.5, and the OLD code with exact refs (`ClaimsPrincipalExtensions.cs` `TryGetOid` L17 / consumers-tenant `9188040d…` L9 / `GetIdentityCandidates` L89 / `GetAccessCandidates` L108 email-alias; `SpaceAccessService.EvaluateAccessAsync` L62 → `SpaceRepository.GetWithOwnerMigrationAsync` L121 "try each stale identity candidate" + `MembershipRepository.FindByCandidatesAsync` L54; `db.ts upsertMicrosoftUser` L2710 = the auto-join weakness) — confirmed **spec-derived rewrite, no salvage, no golden fixtures, no accounting consult**. Scope: one raw-SQL migration (`20260712170000_IdentityLinks` — `identity_links(subject,tenant_id,user_id)` PK`(subject,tenant_id)` + `resolve_identity_link` **security-definer** upsert function, table **ungranted** → app role EXECUTE-only, **no EF entity** so `HasPendingModelChanges()` stays green); `IIdentityResolver` seam (Ledger Infrastructure, Host-consumed EF-free); `RequireSpacePermissionFilter` re-wired to resolve `(subject,tid)`→internal `user_id` before the membership read; `app.current_actor`/membership keyed by internal `user_id`; **provisioning-on-first-contact is link-only, never a membership** (structural auto-join elimination — AC4). Six front-loaded non-accounting decisions for user sign-off: **D-WP13-RESOLVER** (security-definer + ungranted table, WP09 precedent; alt subject-GUC RLS), **D-WP13-PROVISION** (link-only), **D-WP13-ACTOR** (internal `user_id`), **D-WP13-SUBJECT** (prefer `sub` for consumers tenant), **D-WP13-TID** (require `tid`, else 403 `auth.identity_unresolved`), **D-WP13-KEY** (uuid storage). No OpenAPI/TS drift, no `Domain` change, exactly one migration; main ripple = re-keying integration-test membership seeds to the resolved internal `user_id`. 12 concrete ACs. Awaiting user approval → LL Backend Dev. |
| 2026-07-12 | P2-WP10 | LL Architect | Plan drafted → state `planned` but **BLOCKED on B2 + transition fixtures**. [P2-WP10 plan](./plans/P2-WP10-period-lifecycle.md): period-lifecycle commands (create/close/reopen/lock) + onboarding open-period bootstrap (ADR-0005). **Salvage oracle found via old-repo search:** `adminPeriodDataApi.updatePeriodState` transition matrix (`Open↔Closed` allowed; `Locked` terminal; `→Locked` privileged/system-only in OLD) + `periodUtils.getPeriodForDate` **inclusive** endDate — both routed to **LL Fixture Smith as the fixtures-first task** (transition matrix + inclusive-boundary cases under `tests/fixtures/golden/ledger-core/period-transitions/`). Scope: pure `PeriodLifecycle` state machine (Domain); four `period.manage`-gated (D-WP10-PERM, Owner/Admin), idempotent (WP09 seam, D-WP10-IDEM) endpoints + `GET periods`; new migration adds a **GiST `daterange` overlap-exclusion constraint** mirroring the account code-range EXCLUDE (raw SQL, no column change → no model drift, D-WP10-COLUMNS); onboarding `BootstrapOpenPeriodAsync` seam + documented precondition (space-creation wizard is Phase 3); **D-WP10-BOUNDARY** reconciles the flagged `end_exclusive` vs inclusive `EndDate` off-by-one to canonical half-open `[start,end_exclusive)` (fixes `PeriodStateResolver.FindPeriod` + WP05 mapping; accepted-date set unchanged) → unblocks **WP08 I7**; closed/locked periods make WP05 posts reject → unblocks **WP08 I3**. **Non-goals:** M2 equity close engine (`periodCloseEngine.ts`/rollover), FX reval, VAT periods, fiscal-year bulk generation, admin UI, period deletion. **B2 (blocking) → LL Accounting Expert:** B2.1 overlap, B2.2 gaps, B2.3 fiscal structure (arbitrary ranges vs tiling), B2.4 transition legality (user-invokable + irreversible lock; Closed→Open reopen under OR/GeBüV), B2.5 onboarding default. Any B2 divergence from the OLD oracle → new ADR (ADR-0005 pattern) + target-decision test, OLD fixtures byte-unchanged. 12 concrete ACs. Implementation blocked until B2 answered + fixtures captured + user approval. |
|   2 0 2 6 - 0 7 - 1 3   |   P 3 - W P 0 1   |   L L   Q A   R e v i e w e r   |   V e r i f i e d   W P 0 1   v s   p l a n .   D i f f   c o n f i r m e d   w i t h i n   s c o p e   ( a p p / s r c / a p i   u n t o u c h e d ) .   A l l   t e s t   g a t e s   ( V i t e s t   1 2 / 1 2 ,   l a y o u t ,   b o u n d a r i e s ,   l i n t ,   t y p e c h e c k ,   p a g e   b u d g e t ,   a u d i t )   v e r i f i e d   a s   p a s s i n g   c l e a n l y .   F a l l b a c k s   a n d   R e a c t   1 9   t e s t i n g   s e m a n t i c s   p r o p e r l y   g u a r d e d .   S t a t e   $ \ t o $   * * d o n e * * ;   c o d e   i s   r e a d y   f o r   L L   G i t   v i a   M R .   | 
 
 